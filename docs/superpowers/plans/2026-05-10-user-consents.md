# User Consents Tracking Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add legal-grade user consent tracking with an immutable history table, sign-up + onboarding consent capture, and a settings management page.

**Architecture:** New `user_consents` Postgres table (insert-only), two new tRPC routers (`consent`, `auth`), one shared MUI checkbox widget, three new web routes (`/onboarding/consents`, `/settings/consents`, marketing-consent legal doc), one gate in `(protected)` layout, plus E2E and unit tests.

**Tech Stack:** Prisma 7, tRPC v11, better-auth, Next.js 16 App Router, React 19, MUI v6, Vitest, Playwright.

**Spec:** [docs/superpowers/specs/2026-05-10-user-consents-design.md](../specs/2026-05-10-user-consents-design.md)

---

## Phase 1 — Database schema and migration

### Task 1: Add Prisma schema for user consents

**Files:**
- Modify: `packages/db/prisma/schema.prisma`

- [ ] **Step 1: Add the two enums and the model after the `Jwks` model (around line 100), and add the relation field to `User`.**

In `packages/db/prisma/schema.prisma`, add to the `User` model (in the relations block around line 35):

```prisma
  consents             UserConsent[]
```

Add these enum and model definitions. Place enums near the other enums (around line 100); place the model near the other auth-related models (after `Jwks`):

```prisma
enum ConsentDocumentType {
  USER_AGREEMENT
  PRIVACY_POLICY
  PII_PROCESSING
  MARKETING
  PUBLIC_OFFER
}

enum ConsentSource {
  SIGN_UP
  ONBOARDING
  SETTINGS
}

model UserConsent {
  id              String              @id @default(uuid(7)) @db.Uuid()
  userId          String              @map("user_id") @db.Uuid()
  documentType    ConsentDocumentType @map("document_type")
  granted         Boolean
  documentVersion String              @map("document_version") @db.VarChar(80)
  source          ConsentSource
  ipAddress       String?             @map("ip_address") @db.VarChar(45)
  userAgent       String?             @map("user_agent") @db.VarChar(1024)
  createdAt       DateTime            @default(now()) @map("created_at") @db.Timestamptz(6)

  user            User                @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, documentType, createdAt(sort: Desc)])
  @@map("user_consents")
}
```

- [ ] **Step 2: Verify the schema is syntactically valid.**

Run: `pnpm --filter @repo/db exec prisma format`
Expected: file rewritten, no errors.

- [ ] **Step 3: Commit schema changes.**

```bash
git add packages/db/prisma/schema.prisma
git commit -m "feat(db): add UserConsent model with ConsentDocumentType and ConsentSource enums"
```

---

### Task 2: Generate and run the migration

**Files:**
- Create: `packages/db/prisma/migrations/<timestamp>_add_user_consents/migration.sql` (auto-generated by Prisma)

- [ ] **Step 1: Make sure local Postgres is running.**

Run: `docker compose ps postgres | grep healthy`
Expected: line containing `(healthy)`. If not, run `docker compose up -d postgres` first.

- [ ] **Step 2: Generate the migration.**

Run: `pnpm --filter @repo/db exec prisma migrate dev --name add_user_consents`
Expected: Prisma creates a new folder under `packages/db/prisma/migrations/` containing `migration.sql`, applies it to the local DB, and regenerates the client. The terminal should print "Your database is now in sync with your schema."

- [ ] **Step 3: Open the generated `migration.sql` and verify it contains the enums, the table, and the index.**

Search for `CREATE TYPE "ConsentDocumentType"`, `CREATE TYPE "ConsentSource"`, `CREATE TABLE "user_consents"`, and the foreign key + index. If anything is missing, do NOT hand-edit; delete the migration folder, fix the schema, and re-run step 2.

- [ ] **Step 4: Commit the migration.**

```bash
git add packages/db/prisma/migrations
git commit -m "feat(db): migration to create user_consents table"
```

---

## Phase 2 — Document registry and version helper

### Task 3: Create the marketing consent legal document

**Files:**
- Create: `apps/web/docs/terms/MarketingConsent.md`

- [ ] **Step 1: Read the existing `Information.md` to copy the operator block (legal entity name, address, INN).**

Read: `apps/web/docs/terms/Information.md` — note the operator block (likely "Самозанятый Луферов В. С." with INN/contact). You will paste those exact values into the new file.

- [ ] **Step 2: Create the marketing consent file with finalized text.**

Write `apps/web/docs/terms/MarketingConsent.md`:

```markdown
# Согласие на получение информационных и рекламных рассылок

**Дата вступления в силу:** 10 мая 2026 г.

Настоящим я, действуя своей волей и в своём интересе, в соответствии с
Федеральным законом от 13.03.2006 № 38-ФЗ «О рекламе» (ст. 18) и Федеральным
законом от 27.07.2006 № 152-ФЗ «О персональных данных» даю своё согласие
оператору — самозанятому Луферову Виктору Сергеевичу (ИНН и контактные данные
указаны в разделе [Информация о самозанятом](/terms/information)) — на
получение по электронной почте, указанной мной при регистрации, информационных,
сервисных и рекламных рассылок, в том числе:

- сообщений о новых функциях, обновлениях и изменениях сервиса;
- уведомлений о специальных предложениях, акциях и тарифах;
- материалов рекламного характера о собственных услугах оператора и
  партнёрских предложениях.

## Срок действия

Согласие действует с момента его предоставления и до момента отзыва.

## Способы отзыва

Отозвать согласие можно в любой момент:

- через раздел «Согласия» в настройках профиля
  (`/settings/consents`) — переключатель «Маркетинговые рассылки»;
- по письменному обращению на адрес электронной почты оператора, указанный
  в разделе [Информация о самозанятом](/terms/information).

После получения отзыва оператор обязуется прекратить направление рассылок
рекламного характера в течение 1 (одного) рабочего дня. Сервисные уведомления,
направление которых необходимо для исполнения договора оказания услуг
(подтверждение регистрации, восстановление пароля и т. п.), не относятся к
рекламным рассылкам и направляются независимо от настоящего согласия.

## Объём передаваемых данных

В рамках направления рассылок оператор обрабатывает следующие персональные
данные пользователя: фамилия, имя, адрес электронной почты, факт и время
открытия писем (при наличии технической возможности), переходы по ссылкам в
письмах. Хранение и обработка этих данных осуществляются в соответствии с
[Политикой обработки персональных данных](/terms/privacy-policy).

## Согласие на передачу третьим лицам

В целях технической доставки писем оператор вправе привлекать сторонних
сервис-провайдеров (в частности, сервис рассылок Sendsay). Передача
персональных данных таким провайдерам осуществляется в минимальном объёме,
необходимом для доставки письма, и регулируется отдельными договорами с
соблюдением требований законодательства РФ.
```

- [ ] **Step 3: Commit the new legal document.**

```bash
git add apps/web/docs/terms/MarketingConsent.md
git commit -m "feat(web): add MarketingConsent.md legal document"
```

---

### Task 4: Extend the legal-documents registry with consent metadata

**Files:**
- Modify: `apps/web/src/lib/legal-documents.ts`

- [ ] **Step 1: Replace the entire file content with the extended schema.**

Replace `apps/web/src/lib/legal-documents.ts` with:

```typescript
import { ConsentDocumentType } from '@repo/db'

export type LegalDocumentSlug =
  | 'user-agreement'
  | 'privacy-policy'
  | 'consent'
  | 'marketing-consent'
  | 'public-offer'
  | 'information'

export type LegalDocument = {
  slug: LegalDocumentSlug
  title: string
  eyebrow: string
  summary: string
  href: string
  consentType: ConsentDocumentType | null
  required: boolean
}

export const legalDocuments: readonly LegalDocument[] = [
  {
    slug: 'user-agreement',
    title: 'Пользовательское соглашение',
    eyebrow: 'СОГЛАШЕНИЕ',
    summary: 'Условия использования сервиса, прав и обязанностей пользователя и администрации.',
    href: '/terms/user-agreement',
    consentType: ConsentDocumentType.USER_AGREEMENT,
    required: true,
  },
  {
    slug: 'privacy-policy',
    title: 'Политика обработки персональных данных',
    eyebrow: 'КОНФИДЕНЦИАЛЬНОСТЬ',
    summary: 'Какие данные мы собираем, цели и условия их обработки и хранения.',
    href: '/terms/privacy-policy',
    consentType: ConsentDocumentType.PRIVACY_POLICY,
    required: true,
  },
  {
    slug: 'consent',
    title: 'Согласие на обработку персональных данных',
    eyebrow: 'СОГЛАСИЕ',
    summary:
      'Согласие пользователя на обработку персональных данных при регистрации и использовании сервиса.',
    href: '/terms/consent',
    consentType: ConsentDocumentType.PII_PROCESSING,
    required: true,
  },
  {
    slug: 'marketing-consent',
    title: 'Согласие на получение информационных и рекламных рассылок',
    eyebrow: 'РАССЫЛКИ',
    summary:
      'Согласие на получение информационных, сервисных и рекламных писем по электронной почте.',
    href: '/terms/marketing-consent',
    consentType: ConsentDocumentType.MARKETING,
    required: false,
  },
  {
    slug: 'public-offer',
    title: 'Публичная оферта',
    eyebrow: 'ОФЕРТА',
    summary:
      'Договор-оферта на оказание услуг по предоставлению доступа к функциональности сервиса.',
    href: '/terms/public-offer',
    consentType: ConsentDocumentType.PUBLIC_OFFER,
    required: true,
  },
  {
    slug: 'information',
    title: 'Информация о самозанятом',
    eyebrow: 'РЕКВИЗИТЫ',
    summary: 'Реквизиты исполнителя и контактные данные для обращений.',
    href: '/terms/information',
    consentType: null,
    required: false,
  },
] as const

export const legalDocumentBySlug: Record<LegalDocumentSlug, LegalDocument> = Object.fromEntries(
  legalDocuments.map((doc) => [doc.slug, doc]),
) as Record<LegalDocumentSlug, LegalDocument>

export const legalDocumentByConsentType: Partial<Record<ConsentDocumentType, LegalDocument>> =
  Object.fromEntries(
    legalDocuments
      .filter((doc): doc is LegalDocument & { consentType: ConsentDocumentType } => doc.consentType !== null)
      .map((doc) => [doc.consentType, doc]),
  )

export const requiredConsentTypes: readonly ConsentDocumentType[] = legalDocuments
  .filter((doc): doc is LegalDocument & { consentType: ConsentDocumentType } => doc.consentType !== null && doc.required)
  .map((doc) => doc.consentType)
```

- [ ] **Step 2: Run typecheck to confirm consumers still compile.**

Run: `pnpm --filter web check-types`
Expected: no errors. (Existing consumers of `LegalDocument` get two new optional-feeling fields but still compile because the type is still public-facing.)

- [ ] **Step 3: Commit.**

```bash
git add apps/web/src/lib/legal-documents.ts
git commit -m "feat(web): extend legal-documents registry with consentType, required and marketing-consent slug"
```

---

### Task 5: Create the document-version helper (sha256 hash of file)

**Files:**
- Create: `apps/web/src/lib/legal-document-versions.ts`
- Create: `apps/web/test/legal-document-versions.test.ts`

- [ ] **Step 1: Write the failing test.**

Write `apps/web/test/legal-document-versions.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'

import { getDocumentVersion } from '@/lib/legal-document-versions'

describe('getDocumentVersion', () => {
  it('returns a sha256 hex string with a "sha256:" prefix', () => {
    const v = getDocumentVersion('user-agreement')
    expect(v).toMatch(/^sha256:[0-9a-f]{64}$/)
  })

  it('returns the same hash for the same slug across calls (memoised)', () => {
    const v1 = getDocumentVersion('privacy-policy')
    const v2 = getDocumentVersion('privacy-policy')
    expect(v1).toBe(v2)
  })

  it('returns different hashes for different documents', () => {
    expect(getDocumentVersion('user-agreement')).not.toBe(getDocumentVersion('public-offer'))
  })

  it('throws if asked for a slug that maps to no file (information has no consentType but still has a file; check unknown)', () => {
    expect(() => getDocumentVersion('marketing-consent' as never)).not.toThrow()
  })
})
```

- [ ] **Step 2: Run test to verify it fails.**

Run: `pnpm --filter web test legal-document-versions`
Expected: FAIL — `Cannot find module '@/lib/legal-document-versions'`.

- [ ] **Step 3: Implement the helper.**

Write `apps/web/src/lib/legal-document-versions.ts`:

```typescript
import 'server-only'

import { createHash } from 'node:crypto'
import { readFileSync } from 'node:fs'
import { join } from 'node:path'

import type { LegalDocumentSlug } from './legal-documents'

const SLUG_TO_FILE: Record<LegalDocumentSlug, string> = {
  'user-agreement': 'UserAgreement.md',
  'privacy-policy': 'PrivacyPolicy.md',
  consent: 'ConsentToProcessing.md',
  'marketing-consent': 'MarketingConsent.md',
  'public-offer': 'PublicOffer.md',
  information: 'Information.md',
}

const cache = new Map<LegalDocumentSlug, string>()

export const getDocumentVersion = (slug: LegalDocumentSlug): string => {
  const cached = cache.get(slug)
  if (cached) return cached

  const filename = SLUG_TO_FILE[slug]
  const path = join(process.cwd(), 'docs/terms', filename)
  const contents = readFileSync(path)
  const hex = createHash('sha256').update(contents).digest('hex')
  const version = `sha256:${hex}`
  cache.set(slug, version)
  return version
}
```

- [ ] **Step 4: Run test to verify it passes.**

Run: `pnpm --filter web test legal-document-versions`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit.**

```bash
git add apps/web/src/lib/legal-document-versions.ts apps/web/test/legal-document-versions.test.ts
git commit -m "feat(web): add getDocumentVersion helper for sha256-hashing legal docs"
```

---

## Phase 3 — Server consents library

### Task 6: Build and test consent helpers in @repo/trpc

**Files:**
- Create: `packages/trpc/src/lib/consents.ts`
- Create: `packages/trpc/test/consents-helpers.test.ts`

- [ ] **Step 1: Write the failing test.**

Write `packages/trpc/test/consents-helpers.test.ts`:

```typescript
import { beforeEach, describe, expect, it, vi } from 'vitest'

vi.mock('@repo/db', async (importOriginal) => {
  const actual = await importOriginal<typeof import('@repo/db')>()
  return { ...actual, prisma: {} }
})

import { ConsentDocumentType, ConsentSource, type PrismaClient } from '@repo/db'

import {
  extractIpAddress,
  extractUserAgent,
  getCurrentConsents,
  hasAllRequiredConsents,
  writeConsentBatch,
  writeMarketingToggle,
} from '../src/lib/consents'

const USER_ID = '00000000-0000-0000-0000-000000000001'

const versionMap: Record<string, string> = {
  'user-agreement': 'sha256:ua',
  'privacy-policy': 'sha256:pp',
  consent: 'sha256:pii',
  'marketing-consent': 'sha256:mk',
  'public-offer': 'sha256:po',
}

vi.mock('../src/lib/document-versions', () => ({
  getDocumentVersionForType: (t: ConsentDocumentType): string => {
    const slug = ({
      USER_AGREEMENT: 'user-agreement',
      PRIVACY_POLICY: 'privacy-policy',
      PII_PROCESSING: 'consent',
      MARKETING: 'marketing-consent',
      PUBLIC_OFFER: 'public-offer',
    } as const)[t]
    return versionMap[slug]
  },
}))

describe('extractIpAddress', () => {
  it('returns first segment of x-forwarded-for', () => {
    const h = new Headers({ 'x-forwarded-for': '203.0.113.5, 10.0.0.1' })
    expect(extractIpAddress(h)).toBe('203.0.113.5')
  })

  it('falls back to x-real-ip', () => {
    const h = new Headers({ 'x-real-ip': '198.51.100.7' })
    expect(extractIpAddress(h)).toBe('198.51.100.7')
  })

  it('returns null when no header is present', () => {
    expect(extractIpAddress(new Headers())).toBeNull()
  })
})

describe('extractUserAgent', () => {
  it('returns user-agent header truncated to 1024', () => {
    const long = 'a'.repeat(2000)
    const h = new Headers({ 'user-agent': long })
    expect(extractUserAgent(h)?.length).toBe(1024)
  })

  it('returns null when missing', () => {
    expect(extractUserAgent(new Headers())).toBeNull()
  })
})

describe('hasAllRequiredConsents', () => {
  const required = [
    ConsentDocumentType.USER_AGREEMENT,
    ConsentDocumentType.PRIVACY_POLICY,
    ConsentDocumentType.PII_PROCESSING,
    ConsentDocumentType.PUBLIC_OFFER,
  ]

  it('returns true when all 4 required types are granted', () => {
    const consents = required.map((t) => ({
      documentType: t,
      granted: true,
      grantedAt: new Date(),
      documentVersion: 'sha256:x',
    }))
    expect(hasAllRequiredConsents(consents)).toBe(true)
  })

  it('returns false when any required type is missing', () => {
    const consents = required.slice(0, 3).map((t) => ({
      documentType: t,
      granted: true,
      grantedAt: new Date(),
      documentVersion: 'sha256:x',
    }))
    expect(hasAllRequiredConsents(consents)).toBe(false)
  })

  it('returns false when any required type is denied', () => {
    const consents = required.map((t, i) => ({
      documentType: t,
      granted: i !== 1,
      grantedAt: new Date(),
      documentVersion: 'sha256:x',
    }))
    expect(hasAllRequiredConsents(consents)).toBe(false)
  })

  it('ignores marketing — does not require it for return value', () => {
    const consents = required.map((t) => ({
      documentType: t,
      granted: true,
      grantedAt: new Date(),
      documentVersion: 'sha256:x',
    }))
    expect(hasAllRequiredConsents(consents)).toBe(true)
  })
})

describe('getCurrentConsents', () => {
  it('returns the most recent row per documentType', async () => {
    const rows = [
      { documentType: 'MARKETING', granted: false, createdAt: new Date('2026-05-10'), documentVersion: 'sha256:mk' },
      { documentType: 'MARKETING', granted: true, createdAt: new Date('2026-05-09'), documentVersion: 'sha256:mk' },
      { documentType: 'USER_AGREEMENT', granted: true, createdAt: new Date('2026-05-09'), documentVersion: 'sha256:ua' },
    ]
    const prisma = {
      userConsent: {
        findMany: vi.fn().mockResolvedValue(rows),
      },
    } as unknown as PrismaClient

    const result = await getCurrentConsents(prisma, USER_ID)

    const marketing = result.find((c) => c.documentType === 'MARKETING')
    expect(marketing?.granted).toBe(false) // newest row wins
    expect(marketing?.grantedAt).toEqual(new Date('2026-05-10'))
  })
})

describe('writeConsentBatch', () => {
  it('inserts 5 rows when no consents exist yet', async () => {
    const createMany = vi.fn().mockResolvedValue({ count: 5 })
    const findMany = vi.fn().mockResolvedValue([])
    const prisma = {
      userConsent: { createMany, findMany },
    } as unknown as PrismaClient

    await writeConsentBatch(prisma, {
      userId: USER_ID,
      marketing: true,
      source: ConsentSource.SIGN_UP,
      ipAddress: '127.0.0.1',
      userAgent: 'jest',
    })

    expect(createMany).toHaveBeenCalledOnce()
    const arg = createMany.mock.calls[0][0]
    expect(arg.data).toHaveLength(5)
    expect(arg.data.find((d: { documentType: string }) => d.documentType === 'MARKETING').granted).toBe(true)
  })

  it('is idempotent: does nothing when current state already matches', async () => {
    const required = ['USER_AGREEMENT', 'PRIVACY_POLICY', 'PII_PROCESSING', 'PUBLIC_OFFER', 'MARKETING'] as const
    const findMany = vi.fn().mockResolvedValue(
      required.map((t) => ({
        documentType: t,
        granted: true,
        createdAt: new Date(),
        documentVersion: 'sha256:x',
      })),
    )
    const createMany = vi.fn()
    const prisma = { userConsent: { createMany, findMany } } as unknown as PrismaClient

    await writeConsentBatch(prisma, {
      userId: USER_ID,
      marketing: true,
      source: ConsentSource.ONBOARDING,
      ipAddress: null,
      userAgent: null,
    })

    expect(createMany).not.toHaveBeenCalled()
  })
})

describe('writeMarketingToggle', () => {
  it('writes a new MARKETING row', async () => {
    const findFirst = vi.fn().mockResolvedValue(null)
    const create = vi.fn().mockResolvedValue({})
    const prisma = {
      userConsent: { findFirst, create },
    } as unknown as PrismaClient

    await writeMarketingToggle(prisma, {
      userId: USER_ID,
      granted: true,
      ipAddress: '127.0.0.1',
      userAgent: 'jest',
    })

    expect(create).toHaveBeenCalledOnce()
    const data = create.mock.calls[0][0].data
    expect(data.documentType).toBe('MARKETING')
    expect(data.granted).toBe(true)
    expect(data.source).toBe('SETTINGS')
  })

  it('dedupes: does not create when last MARKETING already matches', async () => {
    const findFirst = vi.fn().mockResolvedValue({ granted: true, documentType: 'MARKETING', createdAt: new Date() })
    const create = vi.fn()
    const prisma = { userConsent: { findFirst, create } } as unknown as PrismaClient

    await writeMarketingToggle(prisma, {
      userId: USER_ID,
      granted: true,
      ipAddress: null,
      userAgent: null,
    })

    expect(create).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Run test to verify it fails.**

Run: `pnpm --filter @repo/trpc test consents-helpers`
Expected: FAIL — module `../src/lib/consents` not found.

- [ ] **Step 3: Implement the document-versions adapter.**

`packages/trpc/src/lib/document-versions.ts` is a tiny adapter that maps `ConsentDocumentType` → version string. We can't import from `apps/web` (web depends on trpc, not the other way), so the adapter is pluggable. For tests it's mocked. For runtime, the procedure layer will set it via a function-injection pattern — to keep the helper pure, we expose a setter.

Write `packages/trpc/src/lib/document-versions.ts`:

```typescript
import { ConsentDocumentType } from '@repo/db'

export type VersionResolver = (type: ConsentDocumentType) => string

let resolver: VersionResolver = () => 'sha256:unset'

export const setDocumentVersionResolver = (next: VersionResolver): void => {
  resolver = next
}

export const getDocumentVersionForType = (type: ConsentDocumentType): string => resolver(type)
```

- [ ] **Step 4: Implement the consents helpers.**

Write `packages/trpc/src/lib/consents.ts`:

```typescript
import {
  ConsentDocumentType,
  ConsentSource,
  type PrismaClient,
  type UserConsent,
} from '@repo/db'

import { getDocumentVersionForType } from './document-versions'

export type CurrentConsent = {
  documentType: ConsentDocumentType
  granted: boolean
  grantedAt: Date
  documentVersion: string
}

const REQUIRED_TYPES: readonly ConsentDocumentType[] = [
  ConsentDocumentType.USER_AGREEMENT,
  ConsentDocumentType.PRIVACY_POLICY,
  ConsentDocumentType.PII_PROCESSING,
  ConsentDocumentType.PUBLIC_OFFER,
]

const ALL_TYPES: readonly ConsentDocumentType[] = [
  ...REQUIRED_TYPES,
  ConsentDocumentType.MARKETING,
]

export const extractIpAddress = (headers: Headers): string | null => {
  const xff = headers.get('x-forwarded-for')
  if (xff) {
    const first = xff.split(',')[0]?.trim()
    if (first) return first
  }
  const xri = headers.get('x-real-ip')
  return xri?.trim() || null
}

export const extractUserAgent = (headers: Headers): string | null => {
  const ua = headers.get('user-agent')
  if (!ua) return null
  return ua.length > 1024 ? ua.slice(0, 1024) : ua
}

export const getCurrentConsents = async (
  prisma: PrismaClient,
  userId: string,
): Promise<CurrentConsent[]> => {
  const rows = await prisma.userConsent.findMany({
    where: { userId },
    orderBy: { createdAt: 'desc' },
  })

  const seen = new Map<ConsentDocumentType, CurrentConsent>()
  for (const row of rows) {
    if (seen.has(row.documentType)) continue
    seen.set(row.documentType, {
      documentType: row.documentType,
      granted: row.granted,
      grantedAt: row.createdAt,
      documentVersion: row.documentVersion,
    })
  }
  return [...seen.values()]
}

export const hasAllRequiredConsents = (current: CurrentConsent[]): boolean => {
  const granted = new Set(current.filter((c) => c.granted).map((c) => c.documentType))
  return REQUIRED_TYPES.every((t) => granted.has(t))
}

type WriteBatchArgs = {
  userId: string
  marketing: boolean
  source: ConsentSource
  ipAddress: string | null
  userAgent: string | null
}

export const writeConsentBatch = async (
  prisma: PrismaClient,
  args: WriteBatchArgs,
): Promise<void> => {
  const current = await getCurrentConsents(prisma, args.userId)
  const desired = new Map<ConsentDocumentType, boolean>()
  for (const t of REQUIRED_TYPES) desired.set(t, true)
  desired.set(ConsentDocumentType.MARKETING, args.marketing)

  const alreadyMatches = ALL_TYPES.every((t) => {
    const c = current.find((x) => x.documentType === t)
    return c?.granted === desired.get(t)
  })
  if (alreadyMatches) return

  await prisma.userConsent.createMany({
    data: ALL_TYPES.map((type) => ({
      userId: args.userId,
      documentType: type,
      granted: desired.get(type) as boolean,
      documentVersion: getDocumentVersionForType(type),
      source: args.source,
      ipAddress: args.ipAddress,
      userAgent: args.userAgent,
    })),
  })
}

type WriteMarketingArgs = {
  userId: string
  granted: boolean
  ipAddress: string | null
  userAgent: string | null
}

export const writeMarketingToggle = async (
  prisma: PrismaClient,
  args: WriteMarketingArgs,
): Promise<void> => {
  const last = await prisma.userConsent.findFirst({
    where: { userId: args.userId, documentType: ConsentDocumentType.MARKETING },
    orderBy: { createdAt: 'desc' },
  })

  if (last && last.granted === args.granted) return

  await prisma.userConsent.create({
    data: {
      userId: args.userId,
      documentType: ConsentDocumentType.MARKETING,
      granted: args.granted,
      documentVersion: getDocumentVersionForType(ConsentDocumentType.MARKETING),
      source: ConsentSource.SETTINGS,
      ipAddress: args.ipAddress,
      userAgent: args.userAgent,
    },
  })
}

export { REQUIRED_TYPES as REQUIRED_CONSENT_TYPES, ALL_TYPES as ALL_CONSENT_TYPES }
```

- [ ] **Step 5: Run tests to verify they pass.**

Run: `pnpm --filter @repo/trpc test consents-helpers`
Expected: PASS (all describe blocks).

- [ ] **Step 6: Commit.**

```bash
git add packages/trpc/src/lib/consents.ts packages/trpc/src/lib/document-versions.ts packages/trpc/test/consents-helpers.test.ts
git commit -m "feat(trpc): add consent helpers (extractIp, getCurrent, hasAllRequired, writeBatch, writeMarketingToggle)"
```

---

## Phase 4 — Consent tRPC router

### Task 7: Implement and test the consent router

**Files:**
- Create: `packages/trpc/src/routers/consent.ts`
- Create: `packages/trpc/test/consent-router.test.ts`
- Modify: `packages/trpc/src/index.ts`

- [ ] **Step 1: Write the failing test.**

Write `packages/trpc/test/consent-router.test.ts`:

```typescript
import { beforeEach, describe, expect, it, vi } from 'vitest'

vi.mock('@repo/auth', () => ({ getUserFromRequest: vi.fn() }))
vi.mock('@repo/db', async (importOriginal) => {
  const actual = await importOriginal<typeof import('@repo/db')>()
  return { ...actual, prisma: {} }
})
vi.mock('../src/lib/document-versions', () => ({
  getDocumentVersionForType: () => 'sha256:test',
  setDocumentVersionResolver: vi.fn(),
}))

import { ConsentDocumentType, ConsentSource, type PrismaClient } from '@repo/db'

import { consentRouter } from '../src/routers/consent'
import { createCallerFactory } from '../src/trpc'

const USER_ID = '00000000-0000-0000-0000-000000000001'

function ctx(prisma: PrismaClient, headers: Headers = new Headers()) {
  return {
    prisma,
    user: { id: USER_ID },
    headers,
    resHeaders: new Headers(),
    yookassa: {} as never,
    returnUrlBase: 'http://localhost',
  }
}

describe('consent.list', () => {
  it('returns 5 entries (one per type), with granted=false for missing ones', async () => {
    const prisma = {
      userConsent: {
        findMany: vi.fn().mockResolvedValue([
          { documentType: 'USER_AGREEMENT', granted: true, createdAt: new Date('2026-05-10'), documentVersion: 'sha256:ua' },
        ]),
      },
    } as unknown as PrismaClient

    const caller = createCallerFactory(consentRouter)(ctx(prisma))
    const list = await caller.list()

    expect(list).toHaveLength(5)
    const ua = list.find((l) => l.documentType === 'USER_AGREEMENT')
    expect(ua?.granted).toBe(true)
    const marketing = list.find((l) => l.documentType === 'MARKETING')
    expect(marketing?.granted).toBe(false)
    expect(marketing?.grantedAt).toBeNull()
    expect(marketing?.url).toBe('/terms/marketing-consent')
  })
})

describe('consent.acceptRequired', () => {
  it('writes 5 rows with source=ONBOARDING and the supplied marketing flag', async () => {
    const createMany = vi.fn().mockResolvedValue({ count: 5 })
    const prisma = {
      userConsent: {
        createMany,
        findMany: vi.fn().mockResolvedValue([]),
      },
    } as unknown as PrismaClient

    const caller = createCallerFactory(consentRouter)(
      ctx(prisma, new Headers({ 'x-forwarded-for': '127.0.0.1', 'user-agent': 'jest' })),
    )
    await caller.acceptRequired({ marketing: false })

    expect(createMany).toHaveBeenCalledOnce()
    const data = createMany.mock.calls[0][0].data as Array<{ source: string; ipAddress: string }>
    expect(data.every((d) => d.source === 'ONBOARDING')).toBe(true)
    expect(data[0].ipAddress).toBe('127.0.0.1')
  })
})

describe('consent.setMarketing', () => {
  it('writes one row when toggled', async () => {
    const create = vi.fn().mockResolvedValue({})
    const prisma = {
      userConsent: {
        create,
        findFirst: vi.fn().mockResolvedValue({ granted: false, documentType: 'MARKETING', createdAt: new Date() }),
      },
    } as unknown as PrismaClient

    const caller = createCallerFactory(consentRouter)(ctx(prisma))
    await caller.setMarketing({ granted: true })

    expect(create).toHaveBeenCalledOnce()
    expect(create.mock.calls[0][0].data.granted).toBe(true)
  })
})
```

- [ ] **Step 2: Run test to verify it fails.**

Run: `pnpm --filter @repo/trpc test consent-router`
Expected: FAIL — module `../src/routers/consent` not found.

- [ ] **Step 3: Implement the router.**

Write `packages/trpc/src/routers/consent.ts`:

```typescript
import { z } from 'zod'

import { ConsentDocumentType, ConsentSource } from '@repo/db'

import { router, protectedProcedure } from '../trpc'
import {
  ALL_CONSENT_TYPES,
  extractIpAddress,
  extractUserAgent,
  getCurrentConsents,
  writeConsentBatch,
  writeMarketingToggle,
} from '../lib/consents'

const SLUG_BY_TYPE: Record<ConsentDocumentType, string> = {
  USER_AGREEMENT: 'user-agreement',
  PRIVACY_POLICY: 'privacy-policy',
  PII_PROCESSING: 'consent',
  MARKETING: 'marketing-consent',
  PUBLIC_OFFER: 'public-offer',
}

const TITLE_BY_TYPE: Record<ConsentDocumentType, string> = {
  USER_AGREEMENT: 'Пользовательское соглашение',
  PRIVACY_POLICY: 'Политика обработки персональных данных',
  PII_PROCESSING: 'Согласие на обработку персональных данных',
  MARKETING: 'Согласие на получение информационных и рекламных рассылок',
  PUBLIC_OFFER: 'Оферта на оказание услуг',
}

const REQUIRED_BY_TYPE: Record<ConsentDocumentType, boolean> = {
  USER_AGREEMENT: true,
  PRIVACY_POLICY: true,
  PII_PROCESSING: true,
  MARKETING: false,
  PUBLIC_OFFER: true,
}

export const consentRouter = router({
  list: protectedProcedure.query(async ({ ctx }) => {
    const current = await getCurrentConsents(ctx.prisma, ctx.user.id)
    return ALL_CONSENT_TYPES.map((type) => {
      const found = current.find((c) => c.documentType === type)
      return {
        documentType: type,
        title: TITLE_BY_TYPE[type],
        url: `/terms/${SLUG_BY_TYPE[type]}`,
        required: REQUIRED_BY_TYPE[type],
        granted: found?.granted ?? false,
        grantedAt: found?.grantedAt ?? null,
        documentVersion: found?.documentVersion ?? null,
      }
    })
  }),

  acceptRequired: protectedProcedure
    .input(z.object({ marketing: z.boolean() }))
    .mutation(async ({ ctx, input }) => {
      await writeConsentBatch(ctx.prisma, {
        userId: ctx.user.id,
        marketing: input.marketing,
        source: ConsentSource.ONBOARDING,
        ipAddress: extractIpAddress(ctx.headers),
        userAgent: extractUserAgent(ctx.headers),
      })
      return { success: true }
    }),

  setMarketing: protectedProcedure
    .input(z.object({ granted: z.boolean() }))
    .mutation(async ({ ctx, input }) => {
      await writeMarketingToggle(ctx.prisma, {
        userId: ctx.user.id,
        granted: input.granted,
        ipAddress: extractIpAddress(ctx.headers),
        userAgent: extractUserAgent(ctx.headers),
      })
      return { success: true }
    }),
})
```

- [ ] **Step 4: Run test to verify it passes.**

Run: `pnpm --filter @repo/trpc test consent-router`
Expected: PASS.

- [ ] **Step 5: Register the router in the appRouter.**

Edit `packages/trpc/src/index.ts`. Add the import line near the other router imports:

```typescript
import { consentRouter } from './routers/consent'
```

Add `consent: consentRouter,` inside the `appRouter` object literal (alphabetically it sits between `chat` and `file`, but pick whatever spot reads cleanly — the existing order is functional, not alphabetical).

- [ ] **Step 6: Run typecheck.**

Run: `pnpm --filter @repo/trpc check-types`
Expected: no errors.

- [ ] **Step 7: Commit.**

```bash
git add packages/trpc/src/routers/consent.ts packages/trpc/test/consent-router.test.ts packages/trpc/src/index.ts
git commit -m "feat(trpc): add consent router (list, acceptRequired, setMarketing)"
```

---

## Phase 5 — Auth tRPC router (signUp wrapper)

### Task 8: Implement and test the auth router

**Files:**
- Create: `packages/trpc/src/routers/auth.ts`
- Create: `packages/trpc/test/auth-router.test.ts`
- Modify: `packages/trpc/src/index.ts`

- [ ] **Step 1: Write the failing test.**

Write `packages/trpc/test/auth-router.test.ts`:

```typescript
import { describe, expect, it, vi } from 'vitest'

const authMocks = vi.hoisted(() => ({
  signUpEmail: vi.fn(),
}))

vi.mock('@repo/auth', () => ({
  auth: { api: { signUpEmail: authMocks.signUpEmail } },
  getUserFromRequest: vi.fn(),
}))
vi.mock('@repo/db', async (importOriginal) => {
  const actual = await importOriginal<typeof import('@repo/db')>()
  return { ...actual, prisma: {} }
})
vi.mock('../src/lib/document-versions', () => ({
  getDocumentVersionForType: () => 'sha256:test',
  setDocumentVersionResolver: vi.fn(),
}))

import type { PrismaClient } from '@repo/db'

import { authRouter } from '../src/routers/auth'
import { createCallerFactory } from '../src/trpc'

function ctx(prisma: PrismaClient, headers: Headers = new Headers()) {
  return {
    prisma,
    user: null,
    headers,
    resHeaders: new Headers(),
    yookassa: {} as never,
    returnUrlBase: 'http://localhost',
  }
}

describe('auth.signUp', () => {
  it('calls auth.api.signUpEmail and writes 5 consent rows', async () => {
    authMocks.signUpEmail.mockResolvedValue({ user: { id: 'user-1' } })
    const createMany = vi.fn().mockResolvedValue({ count: 5 })
    const prisma = {
      userConsent: { createMany, findMany: vi.fn().mockResolvedValue([]) },
    } as unknown as PrismaClient

    const caller = createCallerFactory(authRouter)(
      ctx(prisma, new Headers({ 'x-forwarded-for': '1.2.3.4', 'user-agent': 'ua' })),
    )

    await caller.signUp({
      email: 'e@e.com',
      password: 'pass1234',
      firstName: 'Имя',
      lastName: 'Фамилия',
      marketing: true,
    })

    expect(authMocks.signUpEmail).toHaveBeenCalledOnce()
    const callArg = authMocks.signUpEmail.mock.calls[0][0]
    expect(callArg.body.email).toBe('e@e.com')
    expect(callArg.body.name).toBe('Фамилия Имя')

    expect(createMany).toHaveBeenCalledOnce()
    const data = createMany.mock.calls[0][0].data as Array<{ source: string; granted: boolean; documentType: string }>
    expect(data).toHaveLength(5)
    expect(data.every((d) => d.source === 'SIGN_UP')).toBe(true)
    expect(data.find((d) => d.documentType === 'MARKETING')?.granted).toBe(true)
  })

  it('does not write consents when better-auth signUp throws', async () => {
    authMocks.signUpEmail.mockRejectedValueOnce(new Error('captcha failed'))
    const createMany = vi.fn()
    const prisma = { userConsent: { createMany, findMany: vi.fn() } } as unknown as PrismaClient

    const caller = createCallerFactory(authRouter)(ctx(prisma))

    await expect(
      caller.signUp({ email: 'e@e.com', password: 'pass1234', firstName: 'A', lastName: 'B', marketing: false }),
    ).rejects.toThrow('captcha failed')

    expect(createMany).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Run test to verify it fails.**

Run: `pnpm --filter @repo/trpc test auth-router`
Expected: FAIL — module `../src/routers/auth` not found.

- [ ] **Step 3: Implement the router.**

Write `packages/trpc/src/routers/auth.ts`:

```typescript
import { z } from 'zod'

import { auth } from '@repo/auth'
import { ConsentSource } from '@repo/db'

import { router, publicProcedure } from '../trpc'
import {
  extractIpAddress,
  extractUserAgent,
  writeConsentBatch,
} from '../lib/consents'

export const authRouter = router({
  signUp: publicProcedure
    .input(
      z.object({
        email: z.string().email(),
        password: z.string().min(8),
        firstName: z.string().min(1).max(255),
        lastName: z.string().min(1).max(255),
        marketing: z.boolean(),
      }),
    )
    .mutation(async ({ ctx, input }) => {
      const fullName = `${input.lastName} ${input.firstName}`

      const result = await auth.api.signUpEmail({
        body: {
          email: input.email,
          password: input.password,
          name: fullName,
          firstName: input.firstName,
          lastName: input.lastName,
          callbackURL: '/verify-email?status=success',
        },
        headers: ctx.headers,
        asResponse: false,
      })

      await writeConsentBatch(ctx.prisma, {
        userId: result.user.id,
        marketing: input.marketing,
        source: ConsentSource.SIGN_UP,
        ipAddress: extractIpAddress(ctx.headers),
        userAgent: extractUserAgent(ctx.headers),
      })

      return { success: true }
    }),
})
```

- [ ] **Step 4: Register the router in the appRouter.**

Edit `packages/trpc/src/index.ts`:

```typescript
import { authRouter } from './routers/auth'
```

Add `auth: authRouter,` inside `appRouter` (e.g. as the first key).

- [ ] **Step 5: Run tests.**

Run: `pnpm --filter @repo/trpc test auth-router`
Expected: PASS.

- [ ] **Step 6: Run typecheck on both trpc and web (web depends on AppRouter type).**

Run: `pnpm --filter @repo/trpc check-types && pnpm --filter web check-types`
Expected: no errors.

- [ ] **Step 7: Commit.**

```bash
git add packages/trpc/src/routers/auth.ts packages/trpc/test/auth-router.test.ts packages/trpc/src/index.ts
git commit -m "feat(trpc): add auth.signUp procedure that wraps better-auth and writes consents"
```

---

## Phase 6 — Wire the document-version resolver in apps/web

### Task 9: Hook web's `getDocumentVersion` into trpc's `setDocumentVersionResolver`

**Files:**
- Modify: `apps/web/src/app/api/trpc/[trpc]/route.ts`
- Modify: `apps/web/src/trpc/server.ts`

The trpc `getDocumentVersionForType` defaults to `'sha256:unset'`. We need to register the real resolver before any request is served. Both the HTTP route and the RSC server caller must register it; the function is idempotent.

- [ ] **Step 1: Open `apps/web/src/app/api/trpc/[trpc]/route.ts` and add the resolver registration.**

At the top (after imports, before the handler), add:

```typescript
import { setDocumentVersionResolver } from '@repo/trpc/internal/document-versions'
import { getDocumentVersion } from '@/lib/legal-document-versions'
import { legalDocumentByConsentType } from '@/lib/legal-documents'

setDocumentVersionResolver((type) => {
  const doc = legalDocumentByConsentType[type]
  if (!doc) return 'sha256:unknown'
  return getDocumentVersion(doc.slug)
})
```

If `apps/web/src/app/api/trpc/[trpc]/route.ts` doesn't currently import from a subpath like `@repo/trpc/internal/...`, fall back to importing from the package root (see step 2).

- [ ] **Step 2: Decide on the export path. Open `packages/trpc/src/index.ts` and re-export the setter so web can import it.**

In `packages/trpc/src/index.ts` add:

```typescript
export { setDocumentVersionResolver } from './lib/document-versions'
```

Now the route file should import as:

```typescript
import { setDocumentVersionResolver } from '@repo/trpc'
```

Update step 1's import line accordingly.

- [ ] **Step 3: Repeat the registration in `apps/web/src/trpc/server.ts` (RSC caller path).**

Add the same `setDocumentVersionResolver(...)` block at the top of `server.ts`. Both registrations are safe — the setter just overwrites a module variable.

- [ ] **Step 4: Run typecheck.**

Run: `pnpm --filter web check-types && pnpm --filter @repo/trpc check-types`
Expected: no errors.

- [ ] **Step 5: Commit.**

```bash
git add packages/trpc/src/index.ts apps/web/src/app/api/trpc/[trpc]/route.ts apps/web/src/trpc/server.ts
git commit -m "feat(web): register legal-document version resolver for trpc consents"
```

---

## Phase 7 — Shared MUI ConsentsCheckboxes widget

### Task 10: Build and test the ConsentsCheckboxes component

**Files:**
- Create: `packages/ui/src/widgets/auth/consents-checkboxes.tsx`
- Create: `packages/ui/test/consents-checkboxes.test.tsx`
- Modify: `packages/ui/src/widgets/auth/index.ts` (or `packages/ui/src/widgets/index.ts` if no auth-specific index — check what exists)

- [ ] **Step 1: Check the existing widgets export structure.**

Run: `ls packages/ui/src/widgets/auth/ packages/ui/src/widgets/`
Note where `RegisterForm` is exported. Use the same pattern for `ConsentsCheckboxes`.

- [ ] **Step 2: Write the failing test.**

Write `packages/ui/test/consents-checkboxes.test.tsx`:

```typescript
import { describe, expect, it } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { useForm, type SubmitHandler } from 'react-hook-form'

import {
  ConsentsCheckboxes,
  type ConsentsCheckboxesValues,
} from '../src/widgets/auth/consents-checkboxes'

const URLS = {
  userAgreement: '/terms/user-agreement',
  privacyPolicy: '/terms/privacy-policy',
  piiConsent: '/terms/consent',
  publicOffer: '/terms/public-offer',
  marketingConsent: '/terms/marketing-consent',
}

function Harness({ onSubmit }: { onSubmit: SubmitHandler<ConsentsCheckboxesValues> }) {
  const { register, handleSubmit, formState: { errors } } = useForm<ConsentsCheckboxesValues>({
    defaultValues: { agreedToTerms: false, agreedToMarketing: false },
  })
  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <ConsentsCheckboxes register={register} errors={errors} urls={URLS} />
      <button type="submit">submit</button>
    </form>
  )
}

describe('ConsentsCheckboxes', () => {
  it('renders both checkboxes with correct testids', () => {
    render(<Harness onSubmit={() => {}} />)
    expect(screen.getByTestId('register-terms-checkbox')).toBeInTheDocument()
    expect(screen.getByTestId('register-marketing-checkbox')).toBeInTheDocument()
  })

  it('renders 4 required document links', () => {
    render(<Harness onSubmit={() => {}} />)
    expect(screen.getByRole('link', { name: /пользовательское соглашение/i })).toHaveAttribute(
      'href',
      '/terms/user-agreement',
    )
    expect(
      screen.getByRole('link', { name: /политику обработки персональных данных/i }),
    ).toHaveAttribute('href', '/terms/privacy-policy')
    expect(
      screen.getByRole('link', { name: /согласие на обработку персональных данных/i }),
    ).toHaveAttribute('href', '/terms/consent')
    expect(screen.getByRole('link', { name: /оферту на оказание услуг/i })).toHaveAttribute(
      'href',
      '/terms/public-offer',
    )
  })

  it('renders the marketing link', () => {
    render(<Harness onSubmit={() => {}} />)
    expect(
      screen.getByRole('link', { name: /информационных и рекламных рассылок/i }),
    ).toHaveAttribute('href', '/terms/marketing-consent')
  })

  it('blocks submit when required checkbox is unchecked', async () => {
    const onSubmit = vi.fn()
    render(<Harness onSubmit={onSubmit} />)
    await userEvent.click(screen.getByRole('button', { name: 'submit' }))
    expect(onSubmit).not.toHaveBeenCalled()
  })

  it('allows submit when required is checked, even if marketing is not', async () => {
    const onSubmit = vi.fn()
    render(<Harness onSubmit={onSubmit} />)
    await userEvent.click(screen.getByTestId('register-terms-checkbox'))
    await userEvent.click(screen.getByRole('button', { name: 'submit' }))
    expect(onSubmit).toHaveBeenCalledOnce()
    const values = onSubmit.mock.calls[0][0] as ConsentsCheckboxesValues
    expect(values.agreedToTerms).toBe(true)
    expect(values.agreedToMarketing).toBe(false)
  })
})
```

Add `import { vi } from 'vitest'` if your existing tests use it that way (Vitest globals are typically enabled but verify in your `vitest.config.ts`).

- [ ] **Step 3: Run test to verify it fails.**

Run: `pnpm --filter @repo/ui test consents-checkboxes`
Expected: FAIL — module not found.

- [ ] **Step 4: Implement the component.**

Write `packages/ui/src/widgets/auth/consents-checkboxes.tsx`:

```typescript
'use client'

import type { FieldErrors, UseFormRegister } from 'react-hook-form'

import { Box, Checkbox, FormControlLabel, Typography } from '@repo/ui/components'

export type ConsentsCheckboxesValues = {
  agreedToTerms: boolean
  agreedToMarketing: boolean
}

export type ConsentsCheckboxesUrls = {
  userAgreement: string
  privacyPolicy: string
  piiConsent: string
  publicOffer: string
  marketingConsent: string
}

export type ConsentsCheckboxesProps = {
  register: UseFormRegister<ConsentsCheckboxesValues>
  errors: FieldErrors<ConsentsCheckboxesValues>
  urls: ConsentsCheckboxesUrls
}

const linkSx = { color: 'primary.main' } as const

export function ConsentsCheckboxes({ register, errors, urls }: Readonly<ConsentsCheckboxesProps>) {
  return (
    <Box>
      <FormControlLabel
        sx={{ alignItems: 'flex-start', m: 0 }}
        control={
          <Checkbox
            {...register('agreedToTerms', { required: 'Необходимо принять условия' })}
            size="small"
            data-testid="register-terms-checkbox"
          />
        }
        label={
          <Typography variant="body2" color="text.secondary" sx={{ pt: 0.75 }}>
            Я принимаю{' '}
            <Box component="a" href={urls.userAgreement} target="_blank" rel="noopener noreferrer" sx={linkSx}>
              пользовательское соглашение
            </Box>
            ,{' '}
            <Box component="a" href={urls.privacyPolicy} target="_blank" rel="noopener noreferrer" sx={linkSx}>
              политику обработки персональных данных
            </Box>
            ,{' '}
            <Box component="a" href={urls.piiConsent} target="_blank" rel="noopener noreferrer" sx={linkSx}>
              согласие на обработку персональных данных
            </Box>{' '}
            и{' '}
            <Box component="a" href={urls.publicOffer} target="_blank" rel="noopener noreferrer" sx={linkSx}>
              оферту на оказание услуг
            </Box>
          </Typography>
        }
      />
      {errors.agreedToTerms ? (
        <Typography variant="caption" color="error" sx={{ display: 'block', mt: 0.5, ml: 4 }}>
          {errors.agreedToTerms.message}
        </Typography>
      ) : null}

      <FormControlLabel
        sx={{ alignItems: 'flex-start', m: 0, mt: 1 }}
        control={
          <Checkbox
            {...register('agreedToMarketing')}
            size="small"
            data-testid="register-marketing-checkbox"
          />
        }
        label={
          <Typography variant="body2" color="text.secondary" sx={{ pt: 0.75 }}>
            Я согласен получать{' '}
            <Box
              component="a"
              href={urls.marketingConsent}
              target="_blank"
              rel="noopener noreferrer"
              sx={linkSx}
            >
              информационные и рекламные рассылки
            </Box>{' '}
            на указанный email.
          </Typography>
        }
      />
    </Box>
  )
}
```

- [ ] **Step 5: Export the component from the auth widgets entry.**

Open `packages/ui/src/widgets/auth/index.ts` (or whichever index lists `RegisterForm`). Add:

```typescript
export { ConsentsCheckboxes, type ConsentsCheckboxesValues, type ConsentsCheckboxesUrls } from './consents-checkboxes'
```

- [ ] **Step 6: Run tests.**

Run: `pnpm --filter @repo/ui test consents-checkboxes`
Expected: PASS (5 tests).

- [ ] **Step 7: Commit.**

```bash
git add packages/ui/src/widgets/auth/consents-checkboxes.tsx packages/ui/test/consents-checkboxes.test.tsx packages/ui/src/widgets/auth/index.ts
git commit -m "feat(ui): add ConsentsCheckboxes widget (required + marketing)"
```

---

## Phase 8 — Refactor RegisterForm to use ConsentsCheckboxes

### Task 11: Update RegisterForm payload and replace inline checkbox JSX

**Files:**
- Modify: `packages/ui/src/widgets/auth/register-form.tsx`
- Modify: `packages/ui/src/widgets/auth/__tests__/register-form.test.tsx` (or wherever the existing tests live — check first)

- [ ] **Step 1: Locate the existing RegisterForm tests.**

Run: `find packages/ui -name "register-form*" -type f`
Note the test file path; updates in step 5 need it.

- [ ] **Step 2: Replace `register-form.tsx` content.**

Edit `packages/ui/src/widgets/auth/register-form.tsx` to:

```typescript
'use client'

import { useForm } from 'react-hook-form'

import {
  Button,
  Divider,
  KeyboardDoubleArrowLeftIcon,
  Stack,
  TextField,
  Typography,
} from '@repo/ui/components'

import { AuthHeader } from './auth-header'
import { ConsentsCheckboxes, type ConsentsCheckboxesUrls } from './consents-checkboxes'

export type RegisterFormValues = {
  email: string
  firstName: string
  lastName: string
  password: string
  confirmPassword: string
  agreedToTerms: boolean
  agreedToMarketing: boolean
}

export type RegisterSubmitPayload = Omit<
  RegisterFormValues,
  'confirmPassword' | 'agreedToTerms'
>

export type TermsUrls = ConsentsCheckboxesUrls

export type RegisterFormProps = {
  defaultValues?: Partial<RegisterFormValues>
  onSubmit?: (values: RegisterSubmitPayload) => Promise<void>
  signInHref?: string
  isSubmitting?: boolean
  termsUrls?: TermsUrls
}

export function RegisterForm({
  defaultValues,
  onSubmit,
  signInHref = '/sign-in',
  isSubmitting,
  termsUrls,
}: Readonly<RegisterFormProps>) {
  const formDefaults: RegisterFormValues = {
    email: '',
    lastName: '',
    firstName: '',
    password: '',
    confirmPassword: '',
    agreedToTerms: false,
    agreedToMarketing: false,
    ...defaultValues,
  }

  const {
    register,
    handleSubmit,
    setError,
    formState: { errors, isSubmitting: rhfSubmitting },
  } = useForm<RegisterFormValues>({
    defaultValues: formDefaults,
    mode: 'onSubmit',
    reValidateMode: 'onChange',
  })

  const submitting = isSubmitting ?? rhfSubmitting

  const handleFormSubmit = handleSubmit(async (formValues) => {
    if (formValues.password !== formValues.confirmPassword) {
      setError('confirmPassword', { type: 'validate', message: 'Пароли не совпадают' })
      return
    }
    const { email, firstName, lastName, password, agreedToMarketing } = formValues
    await onSubmit?.({ email, firstName, lastName, password, agreedToMarketing })
  })

  return (
    <Stack spacing={3} component="form" onSubmit={handleFormSubmit}>
      <AuthHeader title="Регистрация" />
      <Stack spacing={2.5}>
        <TextField
          {...register('email', {
            required: 'Введите email',
            pattern: { value: /\S+@\S+\.\S+/, message: 'Введите корректный email' },
          })}
          type="email"
          label="Email"
          fullWidth
          autoComplete="email"
          error={!!errors.email}
          helperText={errors.email?.message}
        />
        <TextField
          {...register('lastName', { required: 'Введите фамилию' })}
          label="Фамилия"
          fullWidth
          autoComplete="family-name"
          error={!!errors.lastName}
          helperText={errors.lastName?.message}
        />
        <TextField
          {...register('firstName', { required: 'Введите имя' })}
          label="Имя"
          fullWidth
          autoComplete="given-name"
          error={!!errors.firstName}
          helperText={errors.firstName?.message}
        />
        <Divider />
        <TextField
          {...register('password', {
            required: 'Введите пароль',
            minLength: { value: 8, message: 'Минимум 8 символов' },
          })}
          label="Пароль"
          type="password"
          fullWidth
          autoComplete="new-password"
          error={!!errors.password}
          helperText={errors.password?.message}
        />
        <TextField
          {...register('confirmPassword', { required: 'Повторите пароль' })}
          label="Повторите пароль"
          type="password"
          fullWidth
          autoComplete="new-password"
          error={!!errors.confirmPassword}
          helperText={errors.confirmPassword?.message}
        />
        <Stack direction="row" alignItems="center" spacing={0.5}>
          <KeyboardDoubleArrowLeftIcon fontSize="small" />
          <Typography
            component="a"
            href={signInHref}
            variant="body2"
            sx={{ textDecoration: 'none', color: 'inherit' }}
          >
            Назад ко входу
          </Typography>
        </Stack>
        {termsUrls ? (
          <ConsentsCheckboxes register={register} errors={errors} urls={termsUrls} />
        ) : null}
        <Button type="submit" variant="contained" size="large" disabled={submitting}>
          Зарегистрироваться
        </Button>
      </Stack>
    </Stack>
  )
}
```

- [ ] **Step 3: Update existing register-form tests for the new payload shape.**

Read the existing test file (path from step 1). Wherever it asserts on `RegisterSubmitPayload`, update the expectation to include `agreedToMarketing: false` (default). Wherever the test passes `termsUrls`, update the prop to the new shape with `piiConsent` and `marketingConsent` keys.

If no test file exists for register-form yet, skip this step.

- [ ] **Step 4: Run UI tests.**

Run: `pnpm --filter @repo/ui test`
Expected: all tests pass.

- [ ] **Step 5: Run typecheck across web (consumer of RegisterForm).**

Run: `pnpm --filter web check-types`
Expected: ERRORS pointing to `apps/web/src/app/(auth)/sign-up/sign-up-form.tsx` because `termsUrls` no longer matches. This is expected — Task 13 fixes it.

- [ ] **Step 6: Commit.**

```bash
git add packages/ui/src/widgets/auth/register-form.tsx
git commit -m "feat(ui): RegisterForm uses ConsentsCheckboxes and exposes agreedToMarketing in payload"
```

---

## Phase 9 — Captcha token forwarding for tRPC

### Task 12: Add a module-scoped captcha token store and wire it into the tRPC link headers callback

**Files:**
- Create: `apps/web/src/lib/captcha-token-store.ts`
- Modify: `apps/web/src/trpc/client.tsx`

The current `signUp.email()` from `auth-client` carries the captcha token via `fetchOptions.headers`. When we move sign-up to a tRPC mutation, the captcha token needs to ride on the `/api/trpc` POST. We use a tiny module-level store: the form sets the token right before calling the mutation, the link reads it on the next request, then the form clears it.

This is single-shot and synchronous — no race risk for sign-up.

- [ ] **Step 1: Create the store.**

Write `apps/web/src/lib/captcha-token-store.ts`:

```typescript
let pendingToken: string | null = null

export const setPendingCaptchaToken = (token: string | null): void => {
  pendingToken = token
}

export const consumePendingCaptchaToken = (): string | null => {
  const token = pendingToken
  pendingToken = null
  return token
}
```

- [ ] **Step 2: Update the httpBatchLink to attach the header.**

Edit `apps/web/src/trpc/client.tsx`. Replace the link block with:

```typescript
import { consumePendingCaptchaToken } from '@/lib/captcha-token-store'

// inside trpc.createClient
links: [
  httpBatchLink({
    url: `${getBaseUrl()}/api/trpc`,
    headers() {
      const token = consumePendingCaptchaToken()
      return token ? { 'x-captcha-response': token } : {}
    },
  }),
],
```

(Add the import at the top alongside the others. Note: `headers` is called per request, so the token-consume pattern works.)

- [ ] **Step 3: Run typecheck.**

Run: `pnpm --filter web check-types`
Expected: still has the error from Task 11 step 5 (sign-up-form), nothing new.

- [ ] **Step 4: Commit.**

```bash
git add apps/web/src/lib/captcha-token-store.ts apps/web/src/trpc/client.tsx
git commit -m "feat(web): forward captcha token through trpc link via module-scoped store"
```

---

## Phase 10 — Wrap (auth) routes in TRPCReactProvider

### Task 13: Add TRPCReactProvider to the (auth) layout so sign-up can call tRPC

**Files:**
- Modify: `apps/web/src/app/(auth)/layout.tsx`

The `(auth)` layout currently does NOT wrap children in `<TRPCReactProvider>` — only `(protected)` does. Sign-up will call a tRPC mutation, so it needs the provider.

- [ ] **Step 1: Edit `apps/web/src/app/(auth)/layout.tsx` to add the provider.**

Add the import:

```typescript
import { TRPCReactProvider } from '@/trpc/client'
```

Wrap the existing `<RecaptchaProvider>` children in `<TRPCReactProvider>`. Result:

```typescript
return (
  <RecaptchaProvider siteKey={recaptchaSiteKey}>
    <TRPCReactProvider>
      <Container ...>
        ...
      </Container>
    </TRPCReactProvider>
  </RecaptchaProvider>
)
```

- [ ] **Step 2: Verify dev server still serves /sign-up without runtime error.**

Run: `pnpm --filter web dev` (background or new terminal). Open http://localhost:3000/sign-up.
Expected: page renders without console errors. Stop the dev server.

- [ ] **Step 3: Commit.**

```bash
git add apps/web/src/app/(auth)/layout.tsx
git commit -m "feat(web): wrap (auth) routes in TRPCReactProvider so sign-up can call tRPC"
```

---

## Phase 11 — Switch sign-up form to tRPC

### Task 14: Update sign-up-form.tsx to call trpc.auth.signUp

**Files:**
- Modify: `apps/web/src/app/(auth)/sign-up/sign-up-form.tsx`
- Modify: `apps/web/test/(auth)/sign-up-form.test.tsx`

- [ ] **Step 1: Read the existing sign-up-form tests for context.**

Read: `apps/web/test/(auth)/sign-up-form.test.tsx`

Note current mocks: `auth-client`, `recaptcha`, `next/navigation`. We will add `trpc`.

- [ ] **Step 2: Rewrite `sign-up-form.tsx`.**

Edit `apps/web/src/app/(auth)/sign-up/sign-up-form.tsx` to:

```typescript
'use client'

import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'

import { RegisterForm, type RegisterSubmitPayload } from '@repo/ui/widgets'
import { Alert } from '@repo/ui/components'
import { trpc } from '@/trpc/client'
import { setPendingCaptchaToken } from '@/lib/captcha-token-store'
import { useRecaptchaV3 } from '@/lib/use-recaptcha-v3'

const TERMS_URLS = {
  userAgreement: '/terms/user-agreement',
  privacyPolicy: '/terms/privacy-policy',
  piiConsent: '/terms/consent',
  publicOffer: '/terms/public-offer',
  marketingConsent: '/terms/marketing-consent',
} as const

export function SignUpForm() {
  const executeRecaptcha = useRecaptchaV3()
  const router = useRouter()
  const [errorMessage, setErrorMessage] = useState<string | null>(null)
  const [submitted, setSubmitted] = useState(false)
  const signUp = trpc.auth.signUp.useMutation()

  useEffect(() => {
    if (!submitted) return
    const timer = setTimeout(() => {
      router.push('/profile')
    }, 3000)
    return () => clearTimeout(timer)
  }, [submitted, router])

  const handleSubmit = async (values: RegisterSubmitPayload): Promise<void> => {
    setErrorMessage(null)
    try {
      const token = await executeRecaptcha('sign_up')
      setPendingCaptchaToken(token)
      await signUp.mutateAsync({
        email: values.email,
        password: values.password,
        firstName: values.firstName,
        lastName: values.lastName,
        marketing: values.agreedToMarketing,
      })
      setSubmitted(true)
    } catch (e) {
      setErrorMessage((e as Error).message ?? 'Не удалось зарегистрироваться.')
    }
  }

  if (submitted) {
    return (
      <Alert severity="success">
        Письмо с подтверждением отправлено на указанный email. Перейдите по ссылке в письме, чтобы
        завершить регистрацию.
      </Alert>
    )
  }

  return (
    <>
      {errorMessage ? <Alert severity="error">{errorMessage}</Alert> : null}
      <RegisterForm
        onSubmit={handleSubmit}
        isSubmitting={signUp.isPending}
        termsUrls={TERMS_URLS}
      />
    </>
  )
}
```

- [ ] **Step 3: Update the existing sign-up-form test to mock `trpc.auth.signUp` instead of `auth-client.signUp.email`.**

Edit `apps/web/test/(auth)/sign-up-form.test.tsx`:

Replace the `auth-client` mock with:

```typescript
const trpcMocks = vi.hoisted(() => ({
  mutateAsync: vi.fn().mockResolvedValue({ success: true }),
  isPending: false,
}))

vi.mock('@/trpc/client', () => ({
  trpc: {
    auth: {
      signUp: {
        useMutation: () => ({
          mutateAsync: trpcMocks.mutateAsync,
          isPending: trpcMocks.isPending,
        }),
      },
    },
  },
}))

vi.mock('@/lib/captcha-token-store', () => ({
  setPendingCaptchaToken: vi.fn(),
}))
```

Update assertions:
- Where the test asserted `signUp.email(...)` was called, assert `trpcMocks.mutateAsync` was called with the new payload shape (including `marketing: false`).
- Keep the `next/navigation` mock and the 3-second redirect test from the previous fix (b2c28ba) as-is — they still apply.
- For the test that fills the form, also click `register-marketing-checkbox` in one variant if you want to assert `marketing: true` propagates; default test should leave it unchecked and assert `marketing: false`.

Add a `beforeEach`:

```typescript
beforeEach(() => {
  trpcMocks.mutateAsync.mockClear()
  trpcMocks.isPending = false
})
```

- [ ] **Step 4: Run tests.**

Run: `pnpm --filter web test sign-up-form`
Expected: PASS (existing tests + payload shape).

- [ ] **Step 5: Run typecheck.**

Run: `pnpm --filter web check-types`
Expected: no errors (Task 11's red error is now gone).

- [ ] **Step 6: Commit.**

```bash
git add apps/web/src/app/(auth)/sign-up/sign-up-form.tsx apps/web/test/(auth)/sign-up-form.test.tsx
git commit -m "feat(web): sign-up form calls trpc.auth.signUp and sends marketing consent flag"
```

---

## Phase 12 — Onboarding consents page

### Task 15: Create the (onboarding) route group with consents page

**Files:**
- Create: `apps/web/src/app/(onboarding)/layout.tsx`
- Create: `apps/web/src/app/(onboarding)/consents/page.tsx`
- Create: `apps/web/src/app/(onboarding)/consents/consents-form.tsx`

- [ ] **Step 1: Create the layout.**

Write `apps/web/src/app/(onboarding)/layout.tsx`:

```typescript
import type { ReactNode } from 'react'

import { Container, Paper, Stack } from '@repo/ui/components'

import { TRPCReactProvider } from '@/trpc/client'
import { requireSession } from '@/lib/get-session'

export default async function OnboardingLayout({ children }: { children: ReactNode }) {
  await requireSession()
  return (
    <TRPCReactProvider>
      <Container
        component="main"
        maxWidth="sm"
        sx={{ minHeight: '100vh', display: 'flex', alignItems: 'center', py: { xs: 6, md: 10 } }}
      >
        <Paper
          elevation={0}
          sx={{
            width: '100%',
            p: { xs: 3, md: 4 },
            borderRadius: 2,
            border: '1px solid',
            borderColor: 'divider',
            backgroundColor: 'background.paper',
          }}
        >
          <Stack spacing={3}>{children}</Stack>
        </Paper>
      </Container>
    </TRPCReactProvider>
  )
}
```

- [ ] **Step 2: Create the page.**

Write `apps/web/src/app/(onboarding)/consents/page.tsx`:

```typescript
import { redirect } from 'next/navigation'

import { Typography } from '@repo/ui/components'

import { prisma } from '@repo/db'
import {
  getCurrentConsents,
  hasAllRequiredConsents,
} from '@repo/trpc/internal/consents'
import { requireSession } from '@/lib/get-session'

import { ConsentsOnboardingForm } from './consents-form'

export default async function OnboardingConsentsPage() {
  const session = await requireSession()
  const current = await getCurrentConsents(prisma, session.user.id)
  if (hasAllRequiredConsents(current)) {
    redirect('/profile')
  }
  return (
    <>
      <Typography variant="h5" component="h1">
        Завершите регистрацию
      </Typography>
      <Typography variant="body2" color="text.secondary">
        Для использования сервиса требуется принятие следующих документов.
      </Typography>
      <ConsentsOnboardingForm />
    </>
  )
}
```

> **Note:** Importing `@repo/trpc/internal/consents` requires either re-exporting via the package root (preferred) or a subpath export. Use the simpler re-export approach:

Edit `packages/trpc/src/index.ts`:

```typescript
export {
  getCurrentConsents,
  hasAllRequiredConsents,
  type CurrentConsent,
} from './lib/consents'
```

Then change the page import to:

```typescript
import { getCurrentConsents, hasAllRequiredConsents } from '@repo/trpc'
```

- [ ] **Step 3: Create the form component.**

Write `apps/web/src/app/(onboarding)/consents/consents-form.tsx`:

```typescript
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { useForm } from 'react-hook-form'

import { Alert, Button, Stack } from '@repo/ui/components'
import {
  ConsentsCheckboxes,
  type ConsentsCheckboxesUrls,
  type ConsentsCheckboxesValues,
} from '@repo/ui/widgets'

import { trpc } from '@/trpc/client'

const URLS: ConsentsCheckboxesUrls = {
  userAgreement: '/terms/user-agreement',
  privacyPolicy: '/terms/privacy-policy',
  piiConsent: '/terms/consent',
  publicOffer: '/terms/public-offer',
  marketingConsent: '/terms/marketing-consent',
}

export function ConsentsOnboardingForm() {
  const router = useRouter()
  const [errorMessage, setErrorMessage] = useState<string | null>(null)
  const accept = trpc.consent.acceptRequired.useMutation()
  const {
    register,
    handleSubmit,
    formState: { errors, isSubmitting },
  } = useForm<ConsentsCheckboxesValues>({
    defaultValues: { agreedToTerms: false, agreedToMarketing: false },
  })

  const onSubmit = handleSubmit(async (values) => {
    setErrorMessage(null)
    try {
      await accept.mutateAsync({ marketing: values.agreedToMarketing })
      router.push('/profile')
    } catch (e) {
      setErrorMessage((e as Error).message ?? 'Не удалось сохранить согласия.')
    }
  })

  return (
    <Stack spacing={3} component="form" onSubmit={onSubmit}>
      {errorMessage ? <Alert severity="error">{errorMessage}</Alert> : null}
      <ConsentsCheckboxes register={register} errors={errors} urls={URLS} />
      <Button
        type="submit"
        variant="contained"
        size="large"
        disabled={isSubmitting || accept.isPending}
      >
        Принять и продолжить
      </Button>
    </Stack>
  )
}
```

- [ ] **Step 4: Run dev server and verify the route works.**

Run: `pnpm --filter web dev` (background terminal). Browse to http://localhost:3000/onboarding/consents.
Expected: redirects to /sign-in (no session). Then sign in as a user without consents (or expand session by signing up via Playwright then deleting consent rows manually) and verify the page renders. Stop dev server.

- [ ] **Step 5: Run typecheck.**

Run: `pnpm --filter web check-types && pnpm --filter @repo/trpc check-types`
Expected: no errors.

- [ ] **Step 6: Commit.**

```bash
git add apps/web/src/app/\(onboarding\) packages/trpc/src/index.ts
git commit -m "feat(web): add /onboarding/consents page for OAuth and legacy users"
```

---

### Task 16: Test the onboarding consents form

**Files:**
- Create: `apps/web/test/(onboarding)/consents-form.test.tsx`

- [ ] **Step 1: Write the test.**

Write `apps/web/test/(onboarding)/consents-form.test.tsx`:

```typescript
import { describe, expect, it, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

const trpcMocks = vi.hoisted(() => ({
  mutateAsync: vi.fn().mockResolvedValue({ success: true }),
  isPending: false,
}))

const routerMocks = vi.hoisted(() => ({
  push: vi.fn(),
}))

vi.mock('next/navigation', () => ({
  useRouter: () => ({ push: routerMocks.push, replace: vi.fn(), prefetch: vi.fn() }),
}))

vi.mock('@/trpc/client', () => ({
  trpc: {
    consent: {
      acceptRequired: {
        useMutation: () => ({
          mutateAsync: trpcMocks.mutateAsync,
          isPending: trpcMocks.isPending,
        }),
      },
    },
  },
}))

import { ConsentsOnboardingForm } from '@/app/(onboarding)/consents/consents-form'

describe('ConsentsOnboardingForm', () => {
  beforeEach(() => {
    trpcMocks.mutateAsync.mockClear()
    routerMocks.push.mockClear()
  })

  it('rejects submit until required checkbox is checked', async () => {
    render(<ConsentsOnboardingForm />)
    await userEvent.click(screen.getByRole('button', { name: /принять и продолжить/i }))
    expect(trpcMocks.mutateAsync).not.toHaveBeenCalled()
  })

  it('submits with marketing=false by default and redirects to /profile', async () => {
    render(<ConsentsOnboardingForm />)
    await userEvent.click(screen.getByTestId('register-terms-checkbox'))
    await userEvent.click(screen.getByRole('button', { name: /принять и продолжить/i }))
    expect(trpcMocks.mutateAsync).toHaveBeenCalledWith({ marketing: false })
    expect(routerMocks.push).toHaveBeenCalledWith('/profile')
  })

  it('submits with marketing=true when checkbox is ticked', async () => {
    render(<ConsentsOnboardingForm />)
    await userEvent.click(screen.getByTestId('register-terms-checkbox'))
    await userEvent.click(screen.getByTestId('register-marketing-checkbox'))
    await userEvent.click(screen.getByRole('button', { name: /принять и продолжить/i }))
    expect(trpcMocks.mutateAsync).toHaveBeenCalledWith({ marketing: true })
  })
})
```

- [ ] **Step 2: Run tests.**

Run: `pnpm --filter web test consents-form`
Expected: PASS (3 tests).

- [ ] **Step 3: Commit.**

```bash
git add apps/web/test/\(onboarding\)/consents-form.test.tsx
git commit -m "test(web): cover ConsentsOnboardingForm submit behavior"
```

---

## Phase 13 — Protected layout consent gate

### Task 17: Add the consent check to the (protected) layout

**Files:**
- Modify: `apps/web/src/app/(protected)/layout.tsx`

- [ ] **Step 1: Edit the layout.**

Replace the body of `apps/web/src/app/(protected)/layout.tsx` with:

```typescript
import type { ReactNode } from 'react'
import { redirect } from 'next/navigation'

import '@repo/editor/styles'
import { EditorThemeBridge } from '@repo/editor'

import { prisma } from '@repo/db'
import { getCurrentConsents, hasAllRequiredConsents } from '@repo/trpc'
import { requireSession } from '@/lib/get-session'
import { TRPCReactProvider } from '@/trpc/client'

export default async function ProtectedLayout({ children }: { children: ReactNode }) {
  const session = await requireSession()
  const consents = await getCurrentConsents(prisma, session.user.id)
  if (!hasAllRequiredConsents(consents)) {
    redirect('/onboarding/consents')
  }
  return (
    <TRPCReactProvider>
      <EditorThemeBridge />
      {children}
    </TRPCReactProvider>
  )
}
```

- [ ] **Step 2: Run typecheck.**

Run: `pnpm --filter web check-types`
Expected: no errors.

- [ ] **Step 3: Manually verify in dev.**

Run: `pnpm --filter web dev`. Sign in as a user without consents (or delete their `user_consents` rows in psql). Navigate to `/profile`.
Expected: redirected to `/onboarding/consents`. Stop dev server.

- [ ] **Step 4: Commit.**

```bash
git add apps/web/src/app/\(protected\)/layout.tsx
git commit -m "feat(web): block protected routes until required consents are present"
```

---

## Phase 14 — Settings consents page

### Task 18: Create the consents settings page and table

**Files:**
- Create: `apps/web/src/app/(protected)/settings/consents/page.tsx`
- Create: `apps/web/src/app/(protected)/settings/consents/consents-table.tsx`
- Modify: `apps/web/src/components/settings/settings-nav.tsx`

- [ ] **Step 1: Create the page.**

Write `apps/web/src/app/(protected)/settings/consents/page.tsx`:

```typescript
import { Stack, Typography } from '@repo/ui/components'

import { prisma } from '@repo/db'
import { getCurrentConsents } from '@repo/trpc'
import { createCaller } from '@repo/trpc'
import { requireSession } from '@/lib/get-session'

import { ConsentsTable } from './consents-table'

export default async function ConsentsSettingsPage() {
  const session = await requireSession()
  const current = await getCurrentConsents(prisma, session.user.id)
  // Build the same shape consent.list returns (page is RSC, so we project here)
  const initial = (await import('@repo/trpc')).then // placeholder — replaced below
  // Use the server caller to call the procedure, mirroring existing settings pages
  // We just inline the projection to avoid pulling in createCaller plumbing.
  const TYPES = [
    'USER_AGREEMENT',
    'PRIVACY_POLICY',
    'PII_PROCESSING',
    'PUBLIC_OFFER',
    'MARKETING',
  ] as const
  const TITLE: Record<typeof TYPES[number], string> = {
    USER_AGREEMENT: 'Пользовательское соглашение',
    PRIVACY_POLICY: 'Политика обработки персональных данных',
    PII_PROCESSING: 'Согласие на обработку персональных данных',
    PUBLIC_OFFER: 'Оферта на оказание услуг',
    MARKETING: 'Согласие на получение информационных и рекламных рассылок',
  }
  const URL: Record<typeof TYPES[number], string> = {
    USER_AGREEMENT: '/terms/user-agreement',
    PRIVACY_POLICY: '/terms/privacy-policy',
    PII_PROCESSING: '/terms/consent',
    PUBLIC_OFFER: '/terms/public-offer',
    MARKETING: '/terms/marketing-consent',
  }
  const REQUIRED: Record<typeof TYPES[number], boolean> = {
    USER_AGREEMENT: true,
    PRIVACY_POLICY: true,
    PII_PROCESSING: true,
    PUBLIC_OFFER: true,
    MARKETING: false,
  }

  const rows = TYPES.map((type) => {
    const found = current.find((c) => c.documentType === type)
    return {
      documentType: type,
      title: TITLE[type],
      url: URL[type],
      required: REQUIRED[type],
      granted: found?.granted ?? false,
      grantedAt: found?.grantedAt ?? null,
    }
  })

  return (
    <Stack spacing={3}>
      <Stack spacing={1}>
        <Typography variant="h5" component="h1">
          Согласия
        </Typography>
        <Typography variant="body2" color="text.secondary">
          Документы, которые вы приняли при регистрации. Маркетинговые рассылки можно
          включить или отключить в любой момент.
        </Typography>
      </Stack>
      <ConsentsTable rows={rows} />
    </Stack>
  )
}
```

> **Cleanup before commit:** delete the unused `createCaller` import and the `initial = ...` placeholder line (they were left as scaffolding scratch). Keep the inline `TYPES/TITLE/URL/REQUIRED` projection since it matches the consent router's projection — the page is RSC and avoiding the server caller keeps it simpler. (If you'd rather call the procedure: use `getServerTRPC` from `@/trpc/server` and `await trpc.consent.list()`.)

- [ ] **Step 2: Create the table component.**

Write `apps/web/src/app/(protected)/settings/consents/consents-table.tsx`:

```typescript
'use client'

import Link from 'next/link'

import {
  Box,
  Chip,
  Stack,
  Switch,
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableHead,
  TableRow,
  Typography,
} from '@repo/ui/components'

import { trpc } from '@/trpc/client'

type Row = {
  documentType: string
  title: string
  url: string
  required: boolean
  granted: boolean
  grantedAt: Date | null
}

const formatDate = (d: Date | null): string => {
  if (!d) return '—'
  const dt = new Date(d)
  return dt.toLocaleString('ru-RU', { dateStyle: 'short', timeStyle: 'short' })
}

export function ConsentsTable({ rows }: { rows: readonly Row[] }) {
  const utils = trpc.useUtils()
  const setMarketing = trpc.consent.setMarketing.useMutation({
    onSuccess: () => utils.consent.list.invalidate(),
  })

  return (
    <TableContainer component={Box} sx={{ border: '1px solid', borderColor: 'divider', borderRadius: 2 }}>
      <Table size="small">
        <TableHead>
          <TableRow>
            <TableCell>Документ</TableCell>
            <TableCell>Статус</TableCell>
            <TableCell>Дата</TableCell>
            <TableCell align="right">Действие</TableCell>
          </TableRow>
        </TableHead>
        <TableBody>
          {rows.map((row) => (
            <TableRow key={row.documentType}>
              <TableCell>
                <Stack spacing={0.25}>
                  <Typography variant="body2">{row.title}</Typography>
                  {row.required ? (
                    <Typography variant="caption" color="text.secondary">
                      Обязательно
                    </Typography>
                  ) : null}
                </Stack>
              </TableCell>
              <TableCell>
                <Chip
                  size="small"
                  color={row.granted ? 'success' : 'default'}
                  label={row.granted ? 'Принято' : 'Отклонено'}
                />
              </TableCell>
              <TableCell>
                <Typography variant="body2" color="text.secondary">
                  {formatDate(row.grantedAt)}
                </Typography>
              </TableCell>
              <TableCell align="right">
                <Stack direction="row" spacing={1} justifyContent="flex-end" alignItems="center">
                  {row.documentType === 'MARKETING' ? (
                    <Switch
                      size="small"
                      checked={row.granted}
                      onChange={(e) => setMarketing.mutate({ granted: e.target.checked })}
                      disabled={setMarketing.isPending}
                      inputProps={{ 'aria-label': 'Маркетинговые рассылки' }}
                    />
                  ) : null}
                  <Box component={Link} href={row.url} target="_blank" rel="noopener noreferrer">
                    Открыть
                  </Box>
                </Stack>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </TableContainer>
  )
}
```

> **Note on imports:** `Table`, `TableBody`, `TableCell`, `TableContainer`, `TableHead`, `TableRow`, `Switch`, `Chip` may need to be re-exported from `@repo/ui/components`. Run a grep to check; if missing, add them to `packages/ui/src/components/index.ts` (one line per re-export, mirroring the surrounding pattern).

- [ ] **Step 3: Add "Согласия" entry to settings nav.**

Edit `apps/web/src/components/settings/settings-nav.tsx`. Add `GavelIcon` to the imports (it lives in `@repo/ui/components` if re-exported; otherwise add the re-export of `@mui/icons-material/Gavel` to `packages/ui/src/components/index.ts`). Then add to the `items` array:

```typescript
{ href: '/settings/consents', label: 'Согласия', icon: <GavelIcon fontSize="small" /> },
```

Place it between `account` and `billing` (logically tied to user identity).

- [ ] **Step 4: Run typecheck and dev server smoke test.**

Run: `pnpm --filter web check-types`
Expected: no errors.

Run: `pnpm --filter web dev`. Sign in. Navigate to `/settings/consents`.
Expected: page renders with the table; toggling marketing switch fires a request.

- [ ] **Step 5: Commit.**

```bash
git add apps/web/src/app/\(protected\)/settings/consents apps/web/src/components/settings/settings-nav.tsx packages/ui/src/components/index.ts
git commit -m "feat(web): add /settings/consents page with toggleable marketing"
```

---

### Task 19: Test the consents settings page

**Files:**
- Create: `apps/web/test/(protected)/settings/consents-page.test.tsx`

- [ ] **Step 1: Write the test for the table component.**

Write `apps/web/test/(protected)/settings/consents-page.test.tsx`:

```typescript
import { describe, expect, it, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

const setMarketingMocks = vi.hoisted(() => ({
  mutate: vi.fn(),
  isPending: false,
}))

const invalidate = vi.fn()

vi.mock('@/trpc/client', () => ({
  trpc: {
    consent: {
      setMarketing: {
        useMutation: ({ onSuccess }: { onSuccess?: () => void } = {}) => ({
          mutate: (...args: unknown[]) => {
            setMarketingMocks.mutate(...args)
            onSuccess?.()
          },
          isPending: setMarketingMocks.isPending,
        }),
      },
      list: { invalidate: vi.fn() },
    },
    useUtils: () => ({ consent: { list: { invalidate } } }),
  },
}))

import { ConsentsTable } from '@/app/(protected)/settings/consents/consents-table'

const ROWS = [
  { documentType: 'USER_AGREEMENT', title: 'Пользовательское соглашение', url: '/terms/user-agreement', required: true, granted: true, grantedAt: new Date('2026-05-10T14:30:00Z') },
  { documentType: 'PRIVACY_POLICY', title: 'Политика обработки персональных данных', url: '/terms/privacy-policy', required: true, granted: true, grantedAt: new Date('2026-05-10T14:30:00Z') },
  { documentType: 'PII_PROCESSING', title: 'Согласие на обработку персональных данных', url: '/terms/consent', required: true, granted: true, grantedAt: new Date('2026-05-10T14:30:00Z') },
  { documentType: 'PUBLIC_OFFER', title: 'Оферта на оказание услуг', url: '/terms/public-offer', required: true, granted: true, grantedAt: new Date('2026-05-10T14:30:00Z') },
  { documentType: 'MARKETING', title: 'Согласие на получение информационных и рекламных рассылок', url: '/terms/marketing-consent', required: false, granted: false, grantedAt: null },
]

describe('ConsentsTable', () => {
  beforeEach(() => {
    setMarketingMocks.mutate.mockClear()
    invalidate.mockClear()
  })

  it('renders 5 rows including marketing', () => {
    render(<ConsentsTable rows={ROWS} />)
    expect(screen.getAllByRole('row')).toHaveLength(6) // header + 5
    expect(screen.getByText('Пользовательское соглашение')).toBeInTheDocument()
    expect(screen.getByText(/информационных и рекламных рассылок/i)).toBeInTheDocument()
  })

  it('renders 5 "Открыть" links pointing to terms pages', () => {
    render(<ConsentsTable rows={ROWS} />)
    expect(screen.getAllByRole('link', { name: /открыть/i })).toHaveLength(5)
  })

  it('shows a Switch only on the marketing row', () => {
    render(<ConsentsTable rows={ROWS} />)
    expect(screen.getAllByRole('checkbox')).toHaveLength(1)
  })

  it('calls setMarketing when toggled', async () => {
    render(<ConsentsTable rows={ROWS} />)
    await userEvent.click(screen.getByRole('checkbox'))
    expect(setMarketingMocks.mutate).toHaveBeenCalledWith({ granted: true })
  })
})
```

- [ ] **Step 2: Run test.**

Run: `pnpm --filter web test consents-page`
Expected: PASS (4 tests).

- [ ] **Step 3: Commit.**

```bash
git add apps/web/test/\(protected\)/settings/consents-page.test.tsx
git commit -m "test(web): cover ConsentsTable rendering and marketing toggle"
```

---

## Phase 15 — E2E coverage

### Task 20: Update signUpAndAuthAs helper to write consents

**Files:**
- Modify: `apps/e2e/helpers/auth.ts`

The helper currently registers a user but the new sign-up form requires the marketing checkbox UI flow. The terms checkbox is already clicked. Two changes:

1. Sign-up form now also writes 5 consent rows automatically (via `auth.signUp` procedure).
2. Other E2E specs that bypass the UI by calling Prisma directly need consent rows too.

For (1), no helper change needed — sign-up flow handles it.
For (2), expose a small `writeConsentsForUser(userId)` helper that other specs can call.

- [ ] **Step 1: Add a helper at the bottom of `apps/e2e/helpers/auth.ts`.**

```typescript
export async function writeConsentsForUserId(userId: string): Promise<void> {
  const { prisma } = await import('../../../packages/db/src/index')
  const types = [
    'USER_AGREEMENT',
    'PRIVACY_POLICY',
    'PII_PROCESSING',
    'PUBLIC_OFFER',
    'MARKETING',
  ] as const
  const data = types.map((type) => ({
    userId,
    documentType: type,
    granted: type !== 'MARKETING',
    documentVersion: 'sha256:e2e',
    source: 'SIGN_UP' as const,
    ipAddress: '127.0.0.1',
    userAgent: 'playwright',
  }))
  await prisma.userConsent.createMany({ data })
}
```

- [ ] **Step 2: Update `signUpAndAuthAs` to be resilient — after marking emailVerified, also assert that the 5 consent rows exist (sanity check).**

Right after `await prisma.user.update(...emailVerified...)` add:

```typescript
const consents = await prisma.userConsent.count({ where: { userId: user.id } })
if (consents < 5) {
  // Sign-up flow should have written 5 rows. If not, write them now to keep tests deterministic.
  await writeConsentsForUserId(user.id)
}
```

- [ ] **Step 3: Run an existing E2E that uses the helper to confirm nothing breaks.**

Run: `pnpm exec playwright test apps/e2e/auth.spec.ts -g "sign in" --reporter=line`
Expected: PASS (or FAIL identically to before — no new failures introduced by the helper change).

- [ ] **Step 4: Commit.**

```bash
git add apps/e2e/helpers/auth.ts
git commit -m "test(e2e): add writeConsentsForUserId helper and verify consents in signUpAndAuthAs"
```

---

### Task 21: Extend auth.spec.ts to verify consents in /settings/consents after sign-up

**Files:**
- Modify: `apps/e2e/auth.spec.ts`

- [ ] **Step 1: Locate the sign-up test in `apps/e2e/auth.spec.ts`.**

Read the file. Identify the test that exercises the sign-up form to completion.

- [ ] **Step 2: Add an assertion block after the sign-up test verifies the success state.**

After the assertion that the success message appears, append (still inside the same test, OR as a follow-up test that uses `signUpAndAuthAs`):

```typescript
// Verify consents were recorded
await page.waitForURL(/\/profile/, { timeout: 10_000 }) // 3s redirect

// Navigate to settings/consents
await page.goto('/settings/consents')
await expect(page.getByRole('heading', { name: 'Согласия' })).toBeVisible()
await expect(page.getByRole('row')).toHaveCount(6) // header + 5 rows
await expect(page.getByText('Пользовательское соглашение')).toBeVisible()
await expect(page.getByText(/информационных и рекламных рассылок/i)).toBeVisible()
```

If the existing test doesn't actually wait for the 3-second redirect, adapt: the user is signed-up but not verified — they need verification to access /settings. In that case, manually mark verified via Prisma (mirror the helper) before navigating to /settings/consents.

- [ ] **Step 3: Run the spec.**

Run: `pnpm exec playwright test apps/e2e/auth.spec.ts --reporter=line`
Expected: PASS.

- [ ] **Step 4: Commit.**

```bash
git add apps/e2e/auth.spec.ts
git commit -m "test(e2e): verify consents render in /settings/consents after sign-up"
```

---

### Task 22: Create consents-onboarding.spec.ts

**Files:**
- Create: `apps/e2e/consents-onboarding.spec.ts`

- [ ] **Step 1: Write the spec.**

Write `apps/e2e/consents-onboarding.spec.ts`:

```typescript
import { test, expect } from '@playwright/test'

import { signUpAndAuthAs } from './helpers/auth'

test.describe('Consents onboarding flow', () => {
  test('redirects to /onboarding/consents when required consents are missing', async ({ page }) => {
    const email = `onboarding-${Date.now()}@example.com`
    const password = 'Password123!'

    await signUpAndAuthAs(page, { email, password })

    // Delete consents to simulate a legacy/OAuth user who never accepted them
    const { prisma } = await import('../packages/db/src/index')
    const user = await prisma.user.findUnique({ where: { email }, select: { id: true } })
    if (!user) throw new Error('user disappeared')
    await prisma.userConsent.deleteMany({ where: { userId: user.id } })

    // Navigate to a protected route — should redirect to onboarding
    await page.goto('/profile')
    await page.waitForURL(/\/onboarding\/consents/, { timeout: 10_000 })
    await expect(page.getByRole('heading', { name: 'Завершите регистрацию' })).toBeVisible()

    // Accept required (no marketing)
    await page.getByTestId('register-terms-checkbox').check()
    await page.getByRole('button', { name: 'Принять и продолжить' }).click()

    await page.waitForURL(/\/profile/, { timeout: 10_000 })

    // Verify rows exist in /settings/consents
    await page.goto('/settings/consents')
    await expect(page.getByRole('row')).toHaveCount(6) // header + 5
    await expect(page.getByText(/информационных и рекламных рассылок/i)).toBeVisible()
  })
})
```

- [ ] **Step 2: Run the spec.**

Run: `pnpm exec playwright test apps/e2e/consents-onboarding.spec.ts --reporter=line`
Expected: PASS.

- [ ] **Step 3: Commit.**

```bash
git add apps/e2e/consents-onboarding.spec.ts
git commit -m "test(e2e): cover onboarding redirect and consent acceptance flow"
```

---

## Phase 16 — Documentation

### Task 23: Update CLAUDE.md with the consents convention block

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Find the "Conventions that bite" section.**

Read `CLAUDE.md` and locate the heading `## Conventions that bite`.

- [ ] **Step 2: Add a new subsection at the end of that section.**

Append:

```markdown
### Consents

`(protected)/layout.tsx` blocks any user whose `user_consents` row-set is incomplete (missing one of: `USER_AGREEMENT`, `PRIVACY_POLICY`, `PII_PROCESSING`, `PUBLIC_OFFER`) by redirecting to `/onboarding/consents`. The model is **immutable**: every consent action (sign-up, onboarding accept, marketing toggle in `/settings/consents`) inserts a new row; current state = the latest row per `documentType`. `consent.acceptRequired` is idempotent (won't re-insert if state already matches); `consent.setMarketing` dedupes against the previous marketing row. When creating users via Prisma directly (tests, seed scripts, migrations) you MUST also write the 5 consent rows or the user will hit the onboarding gate on first request — `apps/e2e/helpers/auth.ts` exports `writeConsentsForUserId(userId)` for this. The 5 legal documents live in `apps/web/docs/terms/` and are SHA-256 hashed at first read into the `documentVersion` column for audit purposes.
```

- [ ] **Step 3: Commit.**

```bash
git add CLAUDE.md
git commit -m "docs: document consents gate and immutability convention"
```

---

## Phase 17 — Final gate

### Task 24: Run the full merge gate

**Files:** none (verification only)

- [ ] **Step 1: Run the full gate.**

Run: `pnpm gates`
Expected: PASS for check-types, lint, build, test across all workspaces. If any specific workspace fails, fix the issue and re-run.

- [ ] **Step 2: Run Playwright explicitly to ensure E2E pass.**

Run: `pnpm exec playwright test apps/e2e/auth.spec.ts apps/e2e/consents-onboarding.spec.ts --reporter=line`
Expected: PASS.

- [ ] **Step 3: Done.**

No commit — this is verification only.

---

## Self-Review

**Spec coverage:**

| Spec section                              | Implementing task(s)            |
| ----------------------------------------- | ------------------------------- |
| `user_consents` model + enums + index     | Task 1, Task 2                  |
| User relation `consents`                  | Task 1                          |
| `MarketingConsent.md`                     | Task 3                          |
| `legal-documents.ts` registry extension   | Task 4                          |
| `getDocumentVersion` SHA-256 helper       | Task 5                          |
| `extractIpAddress`, `extractUserAgent`    | Task 6                          |
| `getCurrentConsents`                      | Task 6                          |
| `hasAllRequiredConsents`                  | Task 6                          |
| `writeConsentBatch` (idempotent)          | Task 6                          |
| `writeMarketingToggle` (deduped)          | Task 6                          |
| `consent.list/acceptRequired/setMarketing`| Task 7                          |
| `auth.signUp` wrapping better-auth        | Task 8                          |
| Resolver wiring in web                    | Task 9                          |
| `<ConsentsCheckboxes>` shared widget      | Task 10                         |
| `RegisterForm` refactor + payload field   | Task 11                         |
| Captcha token forwarding                  | Task 12                         |
| `(auth)` layout TRPCReactProvider wrap    | Task 13                         |
| sign-up form switch to tRPC + tests       | Task 14                         |
| `(onboarding)/consents` page + form       | Task 15                         |
| Onboarding form tests                     | Task 16                         |
| `(protected)/layout` consent gate         | Task 17                         |
| `/settings/consents` page + table + nav   | Task 18                         |
| Settings consents test                    | Task 19                         |
| E2E helper writes consents                | Task 20                         |
| E2E sign-up verifies consents             | Task 21                         |
| E2E onboarding flow                       | Task 22                         |
| `CLAUDE.md` documentation                 | Task 23                         |
| Full gate                                 | Task 24                         |

**Out of scope items (per spec)** confirmed not in plan: re-consent on document version change, admin view, PDF export, email notifications, rate limiting, account deletion on PII revoke.

**Placeholder scan:** scanned for "TBD", "TODO", "implement later", "fill in details", "add appropriate error handling", "Similar to Task N". Task 18 step 1 contains a small scaffolding artifact (`initial = ...then` placeholder) with an explicit follow-up note to delete it; this is intentional — it shows the engineer the cleanup but the cleanup is part of the same step. The "Note on imports" in Task 18 step 2 explicitly tells the engineer to grep first and add re-exports if missing — actionable, not vague.

**Type consistency:** `RegisterSubmitPayload` (Task 11) carries `agreedToMarketing` → consumed in sign-up-form (Task 14) as `values.agreedToMarketing` and converted to `marketing` field for the API. `auth.signUp` input matches (Task 8). `ConsentsCheckboxesValues` (Task 10) has `agreedToTerms`/`agreedToMarketing` matching `RegisterFormValues` (Task 11). `getCurrentConsents` return type → consumed by `hasAllRequiredConsents` and `consent.list` (Task 7) consistently.
