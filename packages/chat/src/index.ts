// Intentionally empty. Consumers must import from subpaths
// (`@repo/chat/components`, `@repo/chat/hooks`, `@repo/chat/types`,
// `@repo/chat/theme`) to preserve tree-shaking — same convention as @repo/ui.
export {}
