Вот сжатая версия твоего MD-документа без потери ключевого смысла и требований — убрал дубли, упростил формулировки и оставил только важное 👇

---

# Project Plan: AnyType (Notion-like SaaS)

## Overview

Разработка SaaS-приложения для управления знаниями (аналог Notion / Obsidian):

- Markdown-редактор (block-based, как Notion)
- Работа с медиа (изображения, аудио, PDF и др.)
- Документы как md-страницы
- Авторизация (Yandex + email/password)
- Публичный доступ к документам
- AI-функции (RAG + prompt processing)
- Поиск по контенту
- Темы: light / dark

---

## Tech Stack

- **Frontend**: Next.js 16 + React 19 + MUI + tRPC
- **Backend (AI)**: Python (REST)
- **DB**: PostgreSQL + Prisma
- **Vector DB**: Weaviate
- **Storage**: S3 (MinIO)
- **Cache**: Redis
- **LLM**: Ollama (gemma4)
- **Monorepo**: Turborepo

---

## Monorepo Structure

```
apps/
  web/        # основное приложение (включая лендинг)

packages/
  ui/         # MUI компоненты
  auth/       # авторизация
  db/         # Prisma

infra/
  docker-compose.yml
```

---

## Infrastructure (docker-compose)

Сервисы:

- postgres
- minio
- weaviate
- ollama
- redis

→ все с healthchecks

---

## Core Features

### Auth

- `/sign-in`
- `/registration`
- `/sign-out`

---

### Documents

- Block-based editor (TipTap)
- Markdown поддержка
- Drag & Drop медиа
- Автосохранение
- Иерархия страниц
- Публичный доступ

---

### AI / RAG

- Embeddings документов
- Векторное хранение (Weaviate)
- Семантический поиск
- Prompt execution на документах

---

### Search

- Глобальный поиск (как ChatGPT)
- `/workspaces/{id}/search`

---

## UI / UX

### Темы

- Light / Dark
- Управление через settings

---

### Главная страница (Landing)

Современный продающий дизайн:

- сравнение с Notion / Obsidian
- преимущества
- SaaS vs On-Prem
- тарифы:
  - Free
  - Personal
  - Corporate

- CTA: “купить сейчас”

---

### Footer

Разделы:

- Продукт (docs, pricing, developers)
- Сообщество (contact, roadmap)
- Компания (privacy, terms, offer)

---

## Pages

### About (routes group)

- `/docs` → "Скоро будет документация"
- `/pricing` → тарифы
- `/developers` → интеграции:
  - Yandex
  - AmoCRM
  - Bitrix24
  - MangoOffice

- `/contact` → форма (console.log)
- `/roadmap`:
  - редактор
  - RAG поиск
  - интеграции

- `/privacy`, `/terms`, `/offer` → адаптированные тексты

---

## Settings (route group)

- **General**
  - аватар
  - уведомления
  - тема

- **Account**
  - logout
  - сессии

- **Billing**
  - тариф
  - история

- **Integrations**
  - yandex, github, telegram, amocrm, mango

---

## Workspaces

- 1 workspace на free тарифе
- если нет → `/workspaces/new`
- редирект на default workspace

---

### Workspace Layout

3 колонки:

- Sidebar (240px)
- Content (flex)
- (AI sidebar — УДАЛЕН)

Особенности:

- collapsible sidebar
- минимализм
- onboarding страница (чеклист)
- поддержка тем

---

## Дополнительные требования

- создать стартовую страницу при создании workspace
- убрать лишние UI элементы (AI, Share и т.д.)
- sidebar → иконки при сжатии
- поиск как ChatGPT
- настройки workspace вместо user settings

---

## Database

Нужно:

- смерджить `schema.prisma` + docs/database.md
- добавить:
  - integrations
  - subscriptions (free/personal/corporate)

---

## Dev Tasks

Команды:

```
pnpm run dev
pnpm run lint
pnpm run format
pnpm run check-types
pnpm run build
```

---

## Fixes

- исправить prisma CJS export warning
- добавить `/profile`
- `/settings` → редирект
- облегчить шрифты
- унифицировать темы
- убрать лишние стили
- исправить навигацию

---

## Migration

- выполнить prisma migrate
- подключить к postgres из docker-compose

---

## QA

- проверить через Playwright:
  - авторизацию
  - страницы
  - ошибки в консоли

---

Если хочешь, дальше могу:

- ужать ещё до **чек-листа для разработки (task board)**
- или разложить это в **epics + tickets как в Jira**

---

1. Когда я прехожу на странице с workspace /workspaces/{workspaceId} - выдается 404, а должна открываться страница поиска /workspaces/{workspaceId}/search
2. В разделе поиска есть последних две записи это
3. новый поиск - который создает новый чат - это нужно удалить
4. - Новый чат - который должен делать то, что делает новый поиск, именно он и должен создавать новый чат
5. Кнопка сжатия левого сайдбара должна быть не под корзиной, а там где название workspace, только справа

- Идет иконка
- Дальше идет название рабочего пространства и снизу план
- справа кнопки сжатия

4. Замени все иконки на сайте под material design иконки из mui

- Раздел поиск - SearchIcon
- Настройки - SettingsIcon
- Иконку кордины - DeleteIcon

5. В разделе поиска рядом с названием сделай при наведении появляющуюся три точки и там выбор

- переименовать - тут сделать модальку с переименованием
- удалить - модалка с подтверждением удаления

6. На странице нового чата добавь следующее

- возможность писать многострочный текст, огранич все 10-ю строками
- слева от поля ввода сделай иконку + которое открывает выбор файлов
- справа кнопку отправить занес под поле ввода и замени Отправить на икноку ArrowUpwardIcon from '@mui/icons-material/ArrowUpward'
- иконки сделай реэкспортом из packages/ui

7. Почини хлебные крошки, чтобы они могли отображали

- для Поиска - Поиск > Название раздела для поиска
- для Настроек - Настройки
- Для Страниц - Страницы > {Название папки, если не корневая} > Название страницы
- Для кордины - Корзина

8. На страницы общее не работает смена /settings/general смена темы оформления
9. В настройках измени иконки с эмоций на MUI иконки

- Общее - SettingsIcon
- Аккаунт - PersonIcon
- Оплата - PaymentIcon
- Интеграции - LeakAddIcon

---

Необходимо внести некоторые изменения:

1. В хлебных крошках убери private в конце, эта фраза смущает
2. В поиске кнопка отправить внизу справа, а должна быть на одном уровее с полем ввода
3. Когда мы ходим по разделам сделай подсветку разделов, чтобы понимать где мы находимся
4. Кнопка удалить поисковый чат работает, но чат не удаляет, сделай, чтобы чат удалялся, должна вылезать модалка для подтверждения
5. Сделай так, чтобы можно было переименовывать поисковый чат, переименвоание должно выходить в моделаке
6. Сделай чтобы на любом тарифе можно было переименовывать workspace /workspaces/{workspaceId}/settings/general

---

Необходимо внести некоторые другие изменения

1. Переимнеование должно быть доступно на всех тарифах /workspaces/{workspaceId}/settings/general
2. Удаление рабочего пространства должно быть возможно только овнерам и доступно на всех тарифных планах
3. У поиска измени иконку расркытия на ArrowDropDownIcon

---

Внеси следующие правки

1. Переменование workspace все еще доступно только для тарифов выше free, сделай для всех
   /workspaces/{workespaceId}/settings/general

- убери информационную панель, которая говорит о том, что нужно поднять тариф и сделай возможность менять название

2. Удаление workspace также сделай досупно для всех тарифов /workspaces/{workespaceId}/settings/danger
3. Удалять и переименовывать пространство может только пользователь с ролью OWNER

---

Давай переделаем левый сайдбар

- В нормальном раскрытом состоянии он такой же как и сейчас, поэтому тут ничего не меняй
- для сокрытия используй иконку KeyboardDoubleArrowLeftIcon
- при нажатии не иконку левый сайдбар пропадает, там где хлебные крошки появляется иконка MenuIcon, при нажатии на которую левый сайдбар становиться снова доступен
- при наведении выводится компонент на подобии pooper, который повторяет основной то, что в левом сайд баре, однако находиться поверх контента

---

Давай сделаем раздел Избранное

- раздел появляется, если в избранном есть хотябы одна страница
- для добавления страниц в избранное добавь отдельную которая много ко многим пользователь и страница
- когда пользователь добавит страницу в избранное, то в избранной выводить и дочерние страницы избранной страницы
- если добавляется страница в избранное, которая содержит дечернюю страницу, а потом добавляется дочерняя страница, то она также на первом уровне избранных

---

Сделай ширину левого сайд бара 313 пикселей

---

Давай поменяем компонент отрисовки поисковых чатов, а также страниц.

1. Страницы могут включать другие страницы, как и поисковые чаты могут включать другие чаты.
   Для реализации компонента дерева возьми компонент MUI X Tree View

- документация доступна по сссылке https://mui.com/x/react-tree-view/quickstart/
- добавь компонент в компонент packages/ui
- сделай реэкспорт для использования

2. При наведении на поисковый чат добавь AddIcon слева от MoreHorizIcon

- при нажатии добавляется дочерний чат

3. При наведении на страницу по событию hover должны подсвечиваться две иконки, расположенные справа от названия вертикально

- AddIcon из MUI - для добавления дочерней страницы, т.е. у страниц должно быть parent_id для формирования дерева
- MoreHorizIcon из MUI для действий над страницей со следующими пунктами в меню
  - StarIcon - если в избранном, StarBorderIcon - если страница не в избранном Добавить в избранное
  - разделительная черта
  - LinkIcon - копировать ссылку на страницу
  - ContentCopy - Сделать копию, должна в этом же разделе создаваться полная копия этой страницы
  - DriveFileRenameOutlineIcon - Переименовать. Переименовываем только заголовок
  - MovingIcon - для перенесения страницы по разделам
  - DeleteIcon - переменстить в корзину, а по сути установить Page.deleted_at = now()

3. В разделе корзина все страницы выводятся страницы у которых Page.deleted_at != null (Page.deleted_at - дата удаления) в плоском формате списоком. В списке справа два действия восстановить или удалить:

- RestoreIcon - иконка для восстановления. Если мы нажимаем на икноку для восстанвоелния, Page.deleted_at устанавливается не в null и страница восстанавливается в списке страниц
- DeleteForeverIcon - иконка для удаления навсегда. Если мы нажимаем на иконку удаления, то страница удаляется из базы

4. Давай сделаем раздел Избранное

- раздел появляется, если в избранном есть хотябы одна страница
- для добавления страниц в избранное добавь отдельную которая много ко многим пользователь и страница
- когда пользователь добавит страницу в избранное, то в избранной выводить и дочерние страницы избранной страницы
- если добавляется страница в избранное, которая содержит дечернюю страницу, а потом добавляется дочерняя страница, то она также на первом уровне избранных

---

Давай исправим несколько неточностей

1. ты перевел все на ThreeView, однако в чатах поиска и в страницах я не могу скрывть и раскрывть дочерние страницы, добавь такую возможность
2. в избранном попадают только страницы, которые отмечены как избранные, дочерние страницы этих избранных почему-то не отрисовываются, а должены, страницы появляются только после обновления страницы, а должны сразу
3. Когда мы делаем копию страницы, новое название должно быть "{Название страницы} (копия)"
4. Когда мы нажимаем на переименовать, в поле ввода должно вывыодиться название страницы уже сейчас, кнопка отмены должна быть обычная, а не primary, в поле ввода должно находиться в фокусе
5. При переименовании страницы, название этой же страницы в блоке избраное не изменяется
6. При нажатии на кнопку переменистить страницу появляется ошибка 404
7. Если я нахожусь в корзине и удаляю страницу, в корзине они не появляются, а появляются только после обновления страницы
8. При обновлении названия workspace /workspaces/{workspaceId}/settings/general, при переименовании, название название workspace и иконки не меняется на главное
9. При вводе иконки в переименовании workspace /workspaces/{workspaceId}/settings/general нужно ограничить ввод одной иконки
10. При создании рабочего пространства выходит ошибка

```
Invalid `tx.page.create()` invocation in /Users/victor/Projects/anynote/apps/web/.next/dev/server/chunks/[root-of-the-server]__0-mgbej._.js:569:32 566 } 567 ]; 568 async function seedStartPage(tx, workspaceId, userId) { → 569 const page = await tx.page.create( Foreign key constraint violated on the constraint: `pages_parent_id_fkey`
```

---

Заметил еще некоторое колиечество багов

1. Когда я на странице нажимаю на три точки, а потом выбираю переместить, падает ошибка 404 и выдает такую ошибку

```
heme.alpha is not a function
src/app/error.tsx (12:10) @ RootError

  10 |   reset: () => void
  11 | }) {
> 12 |   return <PageError error={error} reset={reset} />
     |          ^
  13 | }
  14 |
```

2. Когда я скрываю левый сайд бар, левый сайд бар скрывается и тут же появляется попер, а его быть не должно
3. При удалении у кнопки Отмена удрать primary, осатвить ее без цвета
4. При удалении навсегда в корзине кнопку удалить навесить стиль color=warning
5. В разделе избранное если дочерние страницы не избранные, то у дочерних страниц подсвечивается, а сейчас светиться, это ошибка

---

На странице Корзины /workspaces/{workspaceId}/trash добавь справа от Названия страницы DeleteIcon красным цветом при нажатии на которую будет появляться окно подтверждения удаления, а при подтверждении удаления удаляется все из корзины и из базы

---

1. сделай иконку справа, а не рядом с названием страницы
2. возинкает ошибка, исправь ее

```
Hydration failed because the server rendered HTML didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used:
- A server/client branch `if (typeof window !== 'undefined')`.
- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.
```

---

Давай начнем работать с файлами

- в качестве файлового хранилища будем использовать S3
- в compose.yml есть имплементация по работе с s3 minio, создай там bucket storage, если его нет
- добавь в env переменнуе окружения для работы с S3 для подключения
- давай добавим package/storage в котором добавим имплементацию, которая будет закрывать работу с s3
  - должен быть контракт для работы с файлами
  - должна быть сделана имплементация по работе с файлами
- давай добавим таблицу Files для учета загружаемых файлов со следующими полями
  - id uuid
  - name - имя при загрузке
  - ext - расширение файла для отрисовки иконки
  - file_size - размер файла
  - mime_type
  - hash - хеш файла для дедупликации файлов
  - path - путь в s3
  - status - активен/на проверке/удален/архивирован
  - is_public - возможность скачать файл для не авторизованных пользовтелей (bool)
  - created_at - дата тайм загрузки файла
  - updated_at - дата обновления файла
  - download_count - количество скачиваний
  - expires_at - срок доступности файла, если не указан, файл досупен всегда
  - добавь индексы
    - user_id
    - created_at
    - уникальный индекс по (user_id, hash)
  - путь для скачивания генерируется автоматически в засимости от fileRouter
- сделай в trpc fileRouter, который содержит следующие ручки
  - загрузка файла
  - удаление файла (только если я владелец), статус ставится в DELETED
  - скачивание файла - по сути его проксирования из s3
  - изменение у файла name (может менять только тот, кто загрузил)
  - изменение у файла is_public (может менять только тот, кто загрузил)
- сделай загрузку аватара, когда мы загружаем файл
  - файл сохраняется в таблицу Files
  - формируется путь для скачивания файла и он записывается в User.avatar

---

Логирование действий пользователя

---

Давай пристум к детализации блоков на странице

Мы реализуем markdown редактор на страниц с помощью библиотеки tiptap.dev https://github.com/ueberdosis/tiptap, там есть реализация notion like template. Структура block должна быть гибка и формироваться в структуру для tiptap документа. Также мы должны сделать возможность работы множества пользователей над одним документом с помощью библиотеки yjs

Для пакета packages/db

- таблицу с блоками удали, она не нужна, а аткже удали тип BlockType
- таблицу BlockFile нужно также удалить
- добавь в таблицу Page поле content JSONB поле для хранения структуры данных контекнта
- добавь в таблице Page поле contentYjs JSONB поле для хранения yjs состояни приожений
- в таблице Page добавь поле type (database, kanban - на будущее)
  - text - маркдаун текст с помощью tiptap like notion
  - excalidraw - вывод рисунка как в excalidraw
  - database - информация и подключение в таблцие в базе данных, там будет структура данных для подключения к источникам и стягивания
  - kanban - канбан визуализация движения задач
  - form - страница для реализации заполнения форм

- в таблице pages удалить следующие поля
  - isDatabaseRow
  - parentType
  - coverUrl (потом будем делать отдельной задачей)

- добавить таблиц PagesFiles, которая связывает страницы с загружаемыми файлами, вместо BlockFiles
  - pageId
  - fileId

Создать приложение apps/yjs который будет позволять следующее

- одновременное редактирование страниц в двух окнах
- совместное редактирование документа разными пользователями
- новое приложение должно быть написано на ts и содержать npm скрипты для
  - dev
  - build
  - start
  - lint
  - check-types
- также приложение должно позволять работать со страницей типа excalidraw
- приложение должно авторизовывать входящее подключение и не пускать пользователя, который не имеет отношение к страницы

Создай новый пакет packages/editor по аналогии с пакетом package/ui

- добавить пакет tiptap, а также все сопуствующие пакеты для реализации notion like редактора на странице для отображения md документа в формет line notion
  - оснонвые компоненты
  - расширения для форматирования
  - блоки в стиле notion
  - дополнительные элементы
  - блоки и расширения для совместно работы
  - в качестве дополнительных UI инструментов используй пакет packages/ui на основе MUI
- Создай клиентский комонент редактора, чтобы его можно было импортировать в приложение apps/web
- реализуй slash-меню для вставки блоков (заголовки, списки, цитаты, код, разделители, изображения, файлы)
- добавь drag-and-drop для перемещения блоков
- настрой плавающую паель форматирвоания при выделении текста
- добавь поддержку markdown синтаксиса при вводе (например, # для заголовка, > для цитаты)
- реализовать в дополнение компонент для загрузки картинок и файлов в md стиле
- для загрузки файлов используй `@tiptap-codeless/extension-file-upload` — это готовое решение с поддержкой drag & drop, вставки из буфера обмена, превью и изменения размера
- Для загрузки на сервер настрой `storageMode: "custom"`, файлы загружай через apps/web и прикрепляй их к странице
- Для отображения загруженных изображений в редакторе используй стандартное расширение `@tiptap/extension-image`
- Для файлов, не являющихся изображениями, расширение `FileUpload` автоматически отображает карточки с информацией о файле
- Для Tiptap обязательно настрой базовые стили контента, следуя официальному руководству, синхронизируй состояние, чтобы оно работало с глобальной темой

Создай новый пакет packages/excalidraw по аналогии с packages/ui

- установи @excalidraw/excalidraw
- установи пакет для синхронизации с yjs, возьми @timephy/y-excalidraw - исправленную версию y-excalidraw
- добавить пакет для отрисовки Excalidraw и для такого типа страницы вместо MD документа выводить полотно для отображения excalidraw
- добавь компонент Board.tsx для отрисовки доски
  - используй dymanuc imort с ssr:false для отключения серверного рендеринга, потому что Excalidraw использует браузерное API
- сделай так, чтобы компонент занимал всю отведенную ему область где заполняется страница
- синхронизируй с приложением yjs с приложении apps/yjs
- Создай Yjs-документ и привяжи его к холсту через `new ExcalidrawBinding(ydoc, provider.awareness)`
- Для управления загрузкой файлов (изображений) в Excalidraw используй встроенный механизм `Files` и убедись, что они сохраняются на твой медиа-сервер
- Файлы должны сохраняться на сервер через приложение apps/web и присоединяться к page & workspace

Дополнительные указания

- Добавь фабрику отрисовки компонента в зависимости от типа страницы. page.type === 'EXCALIDRAW' рисуем excalidraw редактор, в противном случае по умолчанию редактор tiptap
- сервер должен свое состояние yjs документов в базу данных через prisma в поле
- Убедись, что Yjs-документ не пересоздается при ре-рендерах Next.js (используй `useMemo` или `useState` для инициализации `Y.Doc`)
- Для Excalidraw проверь, что компонент рендерится только на клиенте и имеет корректную высоту

После всех работ

- прогони основные тесты с помощью playwright
- выполни следующие команды
  - pnpm run link
  - pnpm run format
  - pnpm run check-types

---

Давай поправим неточности, которые возникли в рамках реализации фичи

1. Сделай максимальную ширину блока PageType.text 713 пикселей
2. На странице PageType.excalidraw убери название вверху, эта страница будет без названия
3. При обновлении страницы с excalidraw, все то, что нарисовано - пропадает, хотя в момент рендеринга выводится
4. При написании на странице текста PageType == text текст после обновления страницы также пропадает и не сохраняется базу
5. При создании страницы измени Текстовая страница -> Текст
6. Синхронизация между страницами не работает, хотя yjs приложение должно обеспечивать эту синхронизацию

После окончания работ прогони все тесты с помощью playwright

---

Еще пару неточностей, которые обнаружил

1. Когда мы переименовываем страницу, в хлебных крошках название не меняется, также не меняется название страницы на странице
2. Когда я вызываю slach-меню, меня появляется очень далеко от курсора
3. При перетаскивании элментов на странице, текст слетает в лево браузера, сделай, чтобы не выходило за границы поля
4. При наведении на строку появляется 6 точек, слева должна еще появляться AddIcon для того, чтобы повторить Notion like поведение
5. Если текущая строка пустая, значит выводим SlachMenu в этой же строке
6. если текущая строка не пустая создаем новую строку и выводим Slach меню для нее
7. если при нажатии на плюс зажат Options (macOs) или Alt (windows & linux), в таком случае создаем строку выше и выводим Slach меню туда
8. Давай для Slach меню добавим иконки напротив каждого элемента. Иконок нет в mui, создай папку assets в packages/editor и там создай для каждого элемента icon
9. Когда выводишь toogle list то текст становиться на следующую строку, а должен быть на той же строки, где и чекбокс
10. Редактор excalidraw не синхронизирован по теме с приложением, синхронизируй excalidraw по теме с приложением
11. В редакторе на страницах текста при добавлении таблицы я не могу добавлять строки и столбцы, нужно более точное управление таблциой в mmarkdown

---

Еще некотоыре правки

1. В таблице появились плавающее меню, но там на основе иконок не понятно, что означает, детализируй иконки, в mui иконок нет, сгенерируй svg файлы и положи в assets в папку packages/editor

- добавить столбец слева - InsertColumnLeft
- добавить столбец справа - InsertColumnRight
- удалить столбец - DeleteColum
- добавить строку вверх InsertRowUp
- добавить строку ниже InsertRowDown
- удалить строку - DeleteRow
- удалить таблицу - DeleteTable

2. В список задач нужно взять checkbox из MUI
3. В списке задач нужно сделать checkbox по центру к первой строке текста
4. В списке задач если checkbox true, то нужно отображать текст зачеркнутым и текст красить серым
5. Если мы в excalidraw вставляем картинку, то в темной теме она рисуется в негативе, сделай, чтобы картинка при любой теме рисовалась оригинально

---

еще некоторые правки

1. Когда checkbox в списке задач true, он должен краситься в синий цвет
2. В списоке задач текст должен быть серый только тогда, когда checkbox true в остальных случаях у него должны быт стандартные стили
3. в списке задач компонент checkbox не размещен по центру вертикали относительно первой строки, исправь это
4. В Slach menu параграф переименуй в Текст и сделай в качестве иконки T, также перемести в начало Slash меню
5. Когда в Page текст очень много, скрол главного окна пропадает горизонтальной прокрутки нет, верни горизонтальную браузерную прокрутку

---

еще некоторые правки

1. Когда страница загружается необходимо добавить из mui скелетон, который отражает загрузку страницы
2. Давай сделаем заголовок страницы вровень по левому краю с текстовым редактором
3. выведи икноку для страницы из поля icon
4. Добавь фукнционал редактирования названия страницы и иконки на самой странице при нажатии

- когда мы наводим на название страницы, появляется курсор и при нажатии редактировать
- когда мы нажимаем на название страницы, становится поле ввода и мы меняем

5. Добавь в packages/ui компонент emoji-picker-react

- при нажатии на иконку страницы сделай вывод emoji-picker-react а при выборе сохранение в базе данных
- переделай на странице /workspaces/{workspaceId}/settings/general изменение иконки как выбор с помощью компонента emoji-picker-react
- сделай и икноку и название в одну строку по дизайну

6. в консоле вот такая ошибка WebSocket connection to 'ws://localhost:1234/' failed: WebSocket is closed before the connection is established. исправь ее

---

Необходимо сделать Slash-меню в редакторе @packages/editor для расширения функционала текстового редактора на старанице PageType == 'text' каманду для загрузки файлов и картинок

- добавить slash команду image для загрузки изображения, при загрузке появляется поппер на котором который имеет дваTab из @packages/ui
  - в первом табе загрузка файла с компьютера, выбор документа и загрузка через /api/files/upload?workspaceId={workspaceId}
  - во втором табе вставить ссылку на загрузку файла, никакие запросы никуда отправлять ну нужно, просто добавить ссылку на картинку
- добавть slash команду для загрузки файлов, появляется меню, в котором поле для выбора файла и кнопка загрузки
  - в tiptap записывать информацию о файле со ссылкой, размером
  - создать расширение для tiptap для отображения информации по файлам
  - подготовить в packages/editor в папке assets иконки для популярных форматов файлов, которые называются как расширения
    - txt
    - pdf
    - doc
    - docx
    - xls
    - xlsx
    - ppt
    - pptx
    - jpg
    - png
    - gif
    - mp3
    - wav
    - mp4
    - zip
    - rar
    - html
    - csv
- добавить slash команду для ссылки на другую страницу, необходимо вывести меню с поиском по всем страницам и при выборе вставлять ссылку на страницу
  - в tiptap записывать name, pageId и workspaceId с типом PageLink
  - создать расширение для tiptap для рендеринга это в ссылку для перехода на страницу

Список команд, которвые выподятся при нажатии на slach необходимо сгруппировать

- Базовые блоки (все блоки что есть, пойдут в этот блок), в этот блок также добавить ссылку на другую страницу
- Медиа (загрузка картинок, файлов)

После всех работ сделать следующие действия

- с помощью плагина playwright прогнать все тесты по загрукзе картинок и файлов
- проверить команды
  - pnpm run link
  - pnpm run format
  - pnpm run check-types

---

Некоторые правки

1. в slash меню когда я стрелочками перемещую выбор вниз, прокрутка вниз не едет, а должна
2. Добавь еще заголовок 4 уровня, после заголовка 3-го уровня
3. при вставке изображения нет ресайза, добавь ресайз страницы
4. в slash команде сделай возможность загрузки нескольких файлов
5. убери кнопку загрузить, после выбора сразу загружай при загрузке файлов и при загрузке картинок

---

Расширяем функционал редактора

- Теперь давай сделаем slash команду для вставки markdown файла, когда мы вставляем файл, он вствляется на страницу через markdown insert, сделай все на фронтенде, файл на сервер не загружай
- Добавь новый типа блока callout который состоит из двух колонок
  1. эмоция и изменением из emojii picker
  2. текстовое поле воода такое же как и у основного редактора с возможностью вызова slash menu

---

давай сделаем еще некоторые изменения в отображении эмоций страницы

1. Если иконки нет, то мы ее и не выводим ни на странице ни в левом сайдбаре
2. Заголовой старницы сделать на 2 пункта больше и жирным при отображении страницы на странице /workspaces/{workspaceId}/pages/{pageId}
3. Когда у странице нет названия, выводи не "Без названия", а "Новая страница"
4. Если у страницы нет названия, то Новая страница выводиться серым
5. При наведении на название страници /workspaces/{workspaceId}/pages/{pageId} вверху подсвечивать текст Добавить иконку и при нажатии на кнопку открывается emojii picker и происходит выбор и сохранение (поведение как в Notion)
6. Если иконка есть, то при нажатии на иконку появляться emojii picker, ее можно изменить или удалить (расширить emojii picker), если не полчуается тогда добавть меню с кнопками изменить и удалить, добавив к списку соответствующие иконки

---

Давай поменяем визуализацию вставки изображения при нажатии в Slash меню /image

1. Когда мы вводим команду /image не происходит загрузке, а происходит вставка пустого блока
   в этом блоке текст Нажми, чтобы выбрать файл или передащи
2. пустая рамка, которая требует загрузки файла обводиться штриховой рамкой
3. когда мы выбираем файл или перетаскиваем происходит загрука и его отображение
4. когда у нам файл выбран появляться плавающая панель как с текстом, там слудующие иконки

- центрироват по левому краю, использую иконку AlignHorizontalLeftIcon
- центрировать по центру, использую иконку AlignHorizontalCenter
- центрировать по правому краю, использую иконку AlignHorizontalRight
- вертикальная отделительная черта
- Подпись рисунка (Caption), внизу картинки по центру поле ввода подписи рисунка, использую иконку SubtitlesIcon
- вертикальная отделительная черта
- Скачать, использую иконку DownloadIcon
- Заменить, используя иконку CachedIcon, убирай картинку и возвращай в состояние, когда нужно загружать или перетаскивать файл
- вертикальная отделительная черта
- Удалить, иконка DeleteIcon покрасить в красный цвет, при удалении не спрашиват, тут происходит удаление блока

---

Давай будем расширять функционал работы с редактором

1. При установке темной темы в редакторе excalidraw тема меняется у компонентов, а у поля тема не меняется, исправь ошибку
2. При нажатии на DragIndicatorIcon при наведении на строку должно выводиться меню со следующими действиями

- В заголовке меню выводить название элемента, к которому применяется действие
  - "Превратить в" с иконкой SyncAltOutlinedIcon, при выборе выходит меню и можно преобразовать текст в один из следующих блоков
    - text
    - h1
    - h2
    - h3
    - h4
    - маркированный список
    - нумерованный список
    - цитата
    - код
  - "Цвет" с иконкой FormatPaintOutlinedIcon, при выборе выводиься меню, в котором два блока
    - Цвет текста, слева от названия текста выводяться квадратик с цветом внутри
      - По умолчанию - убираются все цвета
      - Серый
      - Коричневый
      - Ораньжевый
      - Желтый
      - Зеленый
      - Голубой
      - Фиолетовый
      - Розовый
      - Красный
    - "Фон", слева от названия текста выводяться квадратик с цветом внутри
      - По умолчанию
      - Серый
      - Коричневый
      - Оранжевый
      - Желтый
      - Зеленый
      - Синий
      - Фиолетовый
      - Розовый
      - Красный
  - Вертикальная черта
  - Дубликат с иконкой ContentCopyIcon, делает полный дубликат блока и вставляет снизу от основного блока
  - Переместить с иконкой ShortcutIcon, выводиться диалоговое окно с выбором страницу, куда переменстить этот блок, блок из оригинальной страницы удаляется, и добавляться в конец страницы, которую выбрали. После этого сразу открвыается страница, на которую выполненно копирование
  - Удалить с DeleteIcon, иконка покрашена в красный цвет, при нажатии блок удаляется без предупреждения

3. Для редактора создать новый тип, который служит для ToogleList как в Notion. Он служит, чтобы скрвыать содержимое контента. Внутри этого блока должна находиться тоже самое, что и в редакторе, должно работать slach menu.
   В скрытом виде выводиться только первая строка а слева кнопка с иконкой ArrowRightOutlinedIcon, когда открываем блок, выводиться весь контент, поворачивается на 90 градусов по часовой с помощью анимации
4. Для редактора создай новый тип HiddenText. Он по умолчанию скрывает текст и иконка VisibilityOffIcon, вместо контекта выводяться звуздочки вместо текста. Когда нажимаем на иконку VisibilityOffIcon, она изменяется и становиться VisibilityIcon, а текст показвыается в том виде как он есть
5. Добавь MoreHorizIcon справа где хлебные крошки, при нажатии на которые появляться меню с действиями, которые применяются к старнице и состоит из следующих элементов

- LinkIcon Копировать ссылку - происходит копирование ссылки на страницу
- ContentCopyIcon - Копия - создается полная копия страницы в этом же разделе, к имени добавляться (копия), функционал такой же как и в MoreHorizIcon в сайдбаре на странице
- ShortcutIcon - Переместить - открывается диалоговое окно со страницами из сайдбара, выбираем куда переменстить страницу, функционал такой же как и в MoreHorizIcon в сайдбаре на странице
- DeleteIcon - Удалить - при удалении страница переноситься в корзину
- вертикальная черта
- HeightIcon - Полноэкранный - тут Switch компонент, если true - в таком случе контент расширяеться на весь экран, иконку поверни на 90 градусов
- ветикальная черта
- PublishIcon - Экспортировать - при нажатии выводиться диалоговое окно где выбирается формат экспорта, при нажатии экспорт скачивается созданный файл, экспорт осуществляеться в
  - PDF
  - markdown
  - hml

6. Слева от MoreHorizOutlinedIcon в хлебных крошках добавь StarIcon - если страница в избранном и при нажатии нужно убрать из избранного, StarBorderIcon - если страница не находиться в избранном и при нажатии страница добавляется в избранное

После всех работ сделать следующие действия

- с помощью плагина playwright прогнать все тесты по загрукзе картинок и файлов
- проверить команды
  - pnpm run link
  - pnpm run format
  - pnpm run check-types

  ***

---

Исправь следующие замечания

1. Левый сайдбар высотой теперь не до низа страницы, а имеет высоту, которая расчитывается от высоты элементов, сделай высотой на все окно как было
2. сожми текст в меню при выборе цвета, сейчас оно слишком грамосткое
3. при переменещнии ничего не выводиться, а должны выводиться сраницы как в левом сайдбаре
4. Компонент переключатель не скрывает контент как должен
5. Обведи новые компоненты скрытия и переключатель рамкой такого же цвета как и файлы
6. В меню превратить в необходимо добавить иконки, которые соответствуют иконкам в Slash меню
7. При дублировании не происходит копирование контента, но оно должно быть
8. При темной теме поле excalidraw все еще бело, но должно быть темное, при этом вставляемые изображения не должны негативить
9. при экспорте страницы в pdf печатается диалоговое окно, а не страница
10. при экспоте в markdown и html между каждой строкой пустая строка, их быть не должно

---

Нужно исправить некоторые замечания

1. При нажатии на три точки в левом сайд баре и выборе дублирования контент страницы не дублируется, а должен исправь это
2. Когда темная тема, excalidwaw рисует линии, у которых цвет по умолчанию черный, а должен быть белый, это нестыковка, исправь и сделай по нормальному линии на темной теме в excalidraw
3. Переключатель содержимого не скрывает весь текст, кроме первой строки, по сути он не работает
4. Когда вставляю скрытый текст, при вставке по умолчанию он должен показвыаться, а уже потом должен быть скрыт, исправь
5. при удалении иконки со страницы, она пропадает в левом сайд баре, а на странице она не пропадает
6. сделай, чтобы контролы управления работали и для внутренних элементов внутри выноски, скрытого текста и переключателя

---

> Скилл для кодекс

1. Изучи текущий проект
2. Отрефактори приложение apps/agents

- пример наполнения проект возьми по ссылке https://github.com/Luferov/yafs
  - yafs -> agents
  - файлы возьми по списоку
    - .dockerignore
    - .gitignore
    - .pre-commit-config.yaml
    - .python-verion
    - .Dockerfile
    - Makefile
    - alembic.ini
    - cli
    - py.typed
    - pytest.ini
  - добавь библиотеку fast-clean и используй ее, как в yafs
  - внутри дирректории agents
    - apps и вутри создай папку chat и внутри перенеси всю бизнес логику для реализации чата на основе langgraph & langchain
      - repositories - все что связано с IO операциями, фабрику чатов также сюда
      - services - всю бизнес логику перенеси сюда
      - use_cases - описание пользоватльских сценариев
      - router.py - тут fastapi ApiRouter
      - depends.py - dishka провайдер
      - enums.py - для enum
      - errors.py - для пользовательских исключений
      - schemas.py - для всех pydantic схем
    - cli - для CLI команд, просто сделай заготовку
    - cmd - точки входа приложения
      - rest - пока только одна точка входа
    - bootstrap.py для инициализации приложения, много чего берется из fast_clean - повтори такое же создание
    - router.py - главный роутер приложения

3. запусти чтобы приложение работало
4. напиши тест, который на основе LLM Ollama, который лежит в compose.yml отвечает на вопросы, сделай интеграционнаый тест

---

Давай сольем приложения apps/engines и apps/indexer в одно приложение apps/engines, написанное на NestJS, которое будет служить бекендом для приложения apps/web

1. Сформируй структуру пустого приложения на NestJS apps/engines
2. Сформируй внутри приложения два модуля

- apps/engines/apps/indexer - приложение для индексации докуентов
  - с помощью пакета @nestjs/schedule раз в минуту вычитывай таблицу для индексаии и отправляй в redis очередь indexing, если page.updated_at + timedelta(5 min) < now() - закидвыаем в outbox_event таблицу, если задача для страницы уже есть, значит пропускаем, испльзуй upsert для исключения конфликтов
  - индексируются только страницы PageType === TEXT && ownership === TEXT
  - с помощью @nestjs/bullmq слушай очередь для индексации indexing и выполняй следующее
- apps/engines/apps/indexer для реализации MCP сервера используй библиоетку @rekog/mcp-nest, который будет содержать следюущие функции
  - создание страницы
  - создание страницы из загружаемого файла
  - изменение страницы
  - получение текста страницы в формате markdown
  - перемещения страницы
  - получение статистики по workspace
    - какие пользователи добавлены
    - сколько страниц каждого типа
    - сколько всего страниц
  - получение списка файлов workspace
  - получение списка файлов на странице
  - получение списка скиллов
  - получение списка агентов
  - получение статистики по странице
    - какой пользователь создал
    - в какую дату создал
    - какой тип страницы
  - загрузить файл на страницу
  - загрузить картинку на страницу
- для взаимодействия с базой данных используй prisma из пакета @packages/db

3. Алгоритм индексации страниц в сервисе apps/engines/apps/indexer

- все записи страницы удаляются из векторной базе данных
- если поле page.content пустое, завершаем операцию
- пробегаемся по всем первого уровня, текста дочерних нод объединяем и группируем склеинный текст общих нод
  - правила индексации по чанкам
  - берем только ноды первого уровня;
  - текст каждой ноды собираем отдельно;
  - каждая нода = отдельный векторный документ.
  - Для каждой ноды:
  - взять все текстовые листья внутри ноды;
  - склеить в plain text;
  - нормализауем текст:
    - lower-case
    - trim
    - схлопывание пробелов
    - удаление служебных символов
    - unicode normalization
    - удаление пустых строк
    - лемматизация
    - stop-words по языку
    - удаление слишком коротких токенов
  - векторизовать;
  - записать в Qdrant
  - в метаданные Qdrant записать следующие поля
    - pageId
    - workspaceId

---

когда я запускаю сервис engines командой pnpm run dev --filter engines

1. парадет ошибка, добавь пакет @nestjs/config и запусти приложение
2. добавь сваггер

---

сервис apps/engines явлеятся mcp сервисом, его должно быть можно вызвать следующим образом

```http
POST http://localhost:8090/api/mcp
Content-Type: application/json
Accept: application/json, text/event-stream
X-User-Id: 019da937-08fb-74c9-bccf-d0d161ac8d85
x-Workspace-Id: 21ab1e33-ac0d-4f03-8f63-8f3f830b0cc1

{
  "id": "0",
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
      "name": "getPageStats",
      "arguments": {
          "pageId": "b040f19a-3c27-499c-8ae0-8e4d5c3279cd"
      }
  }
}
```

переделай структуру так, чтобы

1. он проверял возможность причастности X-User-Id к пространству x-Workspace-Id
2. во всех схеме удалял поля userId & workspaceId и забирал их из заголовков
3. Этот запрос должен вернуть ответ о статистике страницы

---

Есть несколько изменений по apps/web

1. Удали страницу /workspaces/{workspaceId}/chats, удали из сайдбара AI чаты
2. Переименуй search -> chats, /workspaces/{workspaceId}/search/{chatId} -> /workspaces/{workspaceId}/chats/{chatId}
3. При создании нового пространства заполни страницу Welcome to AnyNote с иконкой 👋, внутри сделай контент из списка дел

- [x] Создать аккаунт в AnyNote
- [ ] Нажать где-нибудь внизу `/` и посмотреть элементы
  - [ ] Ввести `/image`, нажать Enter и загрузить свое фото
- [ ] Создать новую страницу в сайдбаре слева 👈

4. На странице настройки AI агента /workspaces/{workspaceId}/settings/ai

- В первом разделе убрать контрол ввода максимальное количество токенов в ответе, удалить это поле из базы, если оно есть
- В первом разделе настройку температуры удалить
- Раздел API ключи провайдеров полностью удалить, удалить trpc методы, которые это использовали
- Раздел скиллы удалить и удалить из базы данных поля
- Раздел переиндексацию удалить и удалить trpc метод с бизнес логикой, которое отвечает за это

5. Из таблицы ai_providers удалить поля default_base_url, docs_url, supports_streaming, supports_tools
6. Из таблицы ai_models удали поля max_output_tokens, supports_function_calling, default_temperature
7. В таблице credential_schema переименуй в connection
8. Из выбранных моделей в сиде оставить только две

- Gigachat
  - slug: gigachat
  - name: GigaCaht
  - connection: {"clientId": "019da3de-19e1-7f92-a0e1-5b90595c8e6c", "clientSecret": "e0762394-8b7c-48d4-84ea-dd3e4e57420b", "scope": "GIGACHAT_API_PERS" }
    модель в ai_models оставь только GigaChat-2
- Ollama
  - slug: ollama
  - name: Ollama
  - connection: {"baseUrl": "http://localhost:11434"}
    модель в ai_models оставь gemma4

8. Удали пакет @repo/chat и все что связано с ним

---

Нужно спроектировать модуль packages/genogram для визуализации генограммы с page.PageType = 'GENOGRAM' на базе React Flow

Контекст:

- генограмма хранится в модели Page
- тип страницы: Page.PageType = 'GENOGRAM'
- модуль должен быть отдельным пакетом packages/genogram
- задача модуля: преобразовывать доменную JSON-структуру генограммы в React Flow nodes/edges и обратно
- важно разделить:
  1. доменную модель генограммы
  2. визуальное представление в React Flow
  3. layout-логику
  4. сериализацию/десериализацию
- Страница с типом Page.PageType = 'GENOGRAM' отсовывается также как и страница с типом EXCALIDRAW

Основная цель:
Создать архитектуру пакета, типы данных, структуру файлов и базовую модель отображения генограммы с учетом всех обозначений и правил ниже.

# ПРАВИЛА ПРЕДМЕТНОЙ ОБЛАСТИ

1. Базовые элементы людей

- большой квадрат — прямой кровный родственник мужского пола
- большой круг — прямая кровная родственница женского пола
- маленький квадрат — брат для прямых кровных родственников
- маленький круг — сестра для прямых кровных родственников
- большой круг с кругом внутри — женщина-владелец генограммы
- большой квадрат с квадратом внутри — мужчина-владелец генограммы
- цифра внутри элемента — порядковый номер партнера
- знак вопроса внутри элемента — неизвестный человек
- внутри фигуры может быть крест — ранняя или трагическая смерть

2. Подписи и текстовая информация (ОЧЕНЬ ВАЖНО)

Размещение текста:

- для БОЛЬШИХ фигур (основные родственники):
  → текст располагается СЛЕВА от фигуры
- для МАЛЕНЬКИХ фигур и крестов:
  → текст располагается СНИЗУ под элементом

Поддерживаемые данные:

Обязательные (важная информация):

- ФИО
- дата рождения
- дата смерти (если умер)
- возраст человека

Дополнительные:

- место рождения
- профессия
- характер (текст или теги)
- зависимости
- болезни

Требования:

- структура должна поддерживать как structured данные, так и произвольные заметки
- формат должен позволять рендерить:
  - краткую подпись
  - расширенную карточку (в будущем)
- данные не должны быть привязаны к layout (позиции рассчитываются отдельно)

3. Специальные элементы

- маленький крест с "A" — аборт
- маленький крест с "B" — выкидыш
- это отдельный тип сущности (не человек)

4. Линии и геометрия

- все линии под прямым углом (step edges)
- исключения: близнецы и двойняшки (диагонали)

5. Родители

- мужчина всегда слева
- женщина всегда справа
- между ними союз (union)

6. Союзы

- официальный брак — сплошная линия
- неофициальный — штриховая
- развод — две наклонные черты
- положение черт определяет, с кем остались дети
- поддержка нескольких партнеров:
  - каждый союз отдельный
  - не reuse линий
  - есть partnerOrder

7. Дети

- от союза вниз идет trunk
- далее горизонтальная sibling-линия
- от нее вниз к каждому ребенку

8. Близнецы и двойняшки

- отдельная сущность birth group:
  - twins:
    - есть горизонтальная линия между детьми
    - диагонали сходятся в одну точку
  - fraternal:
    - нет горизонтальной линии
    - есть общая точка сверху

9. Layout

- поколения на одной линии
- выравнивание по центру
- layout отдельно от UI
- поддержка:
  - multiple partners
  - children groups
  - twins

# ЧТО НУЖНО СПРОЕКТИРОВАТЬ

1. Основные компоненты:

- PersonNode
- PregnancyLossNode
- UnionNode
- ChildrenHubNode
- BirthGroupNode

2. Типы связей:

- официальный союз
- неофициальный союз
- связь союз → children hub
- sibling line
- child connection
- twin connection
- fraternal connection
- divorce marker
- death marker

3. Структура пакета:
   packages/genogram:

- types
- model
- react-flow
- nodes
- edges
- layout
- transforms
- store
- utils
- hooks

4. TypeScript-типы:
   Нужно описать:

- GenogramPageData
- Person (с полной структурой bio + meta)
- PersonLabel (отдельно!)
- Union
- ChildGroup
- BirthGroup
- PregnancyLoss
- Annotation / Note
- LayoutMetadata (опционально)
- ReactFlowNodeData
- ReactFlowEdgeData
  Особое внимание:
  → Person должен содержать:
  - identity (ФИО)
  - lifeDates (birth/death)
  - computed age (не хранить напрямую, но предусмотреть)
  - profile (профессия, характер, болезни и т.д.)
  - labelConfig (позиция, формат отображения)

5. JSON-структура Page.content:

- version
- entities (people, unions, groups)
- relations
- labels
- viewport (опционально)
- layout metadata (опционально)

6. Transform слой:

- Page.content -> domain
- domain -> React Flow
- React Flow -> domain
- domain -> Page.content

Важно:

- React Flow НЕ является source of truth
- source of truth = domain JSON

7. Публичный API:

- GenogramFlow (React component)
- nodeTypes
- edgeTypes
- types
- factories
- validators
- transforms

8. Архитектура:

- четкое разделение:
  domain / layout / rendering
- React Flow — только визуализация
- поддержка readonly + editor режимов
- расширяемость

# ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

Ответ должен содержать:

1. список компонентов
2. список типов связей
3. структуру пакета
4. ключевые TypeScript-типы
5. JSON-схему
6. transforms
7. API пакета
8. рекомендации по моделированию

# КЛЮЧЕВОЙ АКЦЕНТ

Особенно важно корректно смоделировать:

- подписи (position-aware: left/bottom)
- multiple partners
- разводы
- twins / fraternal
- pregnancy loss
- death markers

Решение должно быть максимально практичным для реальной реализации

---

> промпт для CODEX

1. Необходим создать новый модуль в packages/ui (@repo/chat), который предсталвяет интерфейс интеллектуального агента modern ui assistant:

- как базовый используй пакет @mui/x-chat
- строка ввода позволяет вводить только простой текст и при увеличении количества строк растягивает по вертикали вверх до 12 строк максимум
- весь исходный код для компонентов чата размести в packages/ui/src/components
- необходимо предусомтреть возможность вывода как ChatMessage.parts
  - markdown type: 'text'
  - файлы type: 'file'
- в поле ввода чата должна быть возмножсть прикреплять файлы и удалять
  - загрузка файлов через сервис apps/web по пути /api/files/upload, файлы должны быть привязаны к чату
  - файлы прикрепляются, но мы их пока никуда не отправляем, просто прикрепляем
- новый дизайн чата с помощью @mui/x-chat необходимо заменить на странице /workspaces/{workspaceId}/chats/{chatId} убрав старые компоненты
  Созданный чат должен позволять выводить информация из стриминга и по вызову тулинга, подтвержедния

2. При отправки сообщения в чате на странице /workspaces/{workspaceId}/chats/{chatId} должно происходить следующее

- сообщение сохраняется в chatMessage, загруженные файлы прикрепляются к сообщению в таблицу ChatMessageFile, сообщение выводиться в чате как ChatMessage.parts
- мы отправляем на apps/web, который в свою очередь проксирует запрос в сервис apps/agents на роут /api/generate и стримит данные на клиент в чат, сохраняя при этом данные ответа от /api/generate в базу данных, если произошла перезагрузка страницы, данные оставались
- запрос из apps/web при отправке сообщения в сервис apps/agents выглядит так, правила заполнения через # для текущего чата и для текущего workspaceId взятого из чата. Ниже представлен пример запроса

```http
POST http://localhost:8080/chat/generate HTTP/1.1
content-type: application/json
x-user-id: 123e4567-e89b-12d3-a456-426614174000
x-workspace-id: 123e4567-e89b-12d3-a456-426614174001

{
  "threadId": "3fa85f64-5717-4562-b3fc-2c963f66afa6", # Chat.id - по сути чат, в который мы пишем
  "model": {
    "provider": "gigachat", # выбранная модель для AiProvider.slug
    "name": "GigaChat-2",   # AiModel.slug
    "connection": { # AiProvider.connection
      "clientId": "019da3de-19e1-7f92-a0e1-5b90595c8e6c",
      "clientSecret": "e0762394-8b7c-48d4-84ea-dd3e4e57420b",
      "scope": "GIGACHAT_API_PERS"
    },
    "settings": {
      "temperature": 0, # WorkspaceAiSettings.temperature
      "topP": 0         # WorkspaceAiSettings.temperature
    }
  },
  "systemPrompt": "Ты профессиональный помощник, который помогает пользователю с его запросами. Ты всегда отвечаешь на русском языке.", # WorkspaceAiSettings.systemPrompt
  "mcp": {
    "servers": [{
      "name": "AnyNote MCP Server",
      "url": "http://localhost:8090/api/mcp",
      "headers": {
        "Content-Type": "application/json",
        "Accept": "application/json, text/event-stream",
        "X-User-Id": "019da937-08fb-74c9-bccf-d0d161ac8d85", # Chat.userId
        "x-Workspace-Id": "21ab1e33-ac0d-4f03-8f63-8f3f830b0cc1" # Chat.workspaceId
      },
      "retries": 3,
      "verify": false
    }]
  },
  "instruction": { # без изменений стандартный
    "format": "markdown",
    "language": "ru",
    "citationsRequired": true
  },
  "query": "Какие возможности у AnyNote MCP Server?" # запрос пользователя в чате
}
```

- по умолчанию из коробки идет один mcpServer - AnyNote MCP сервер, поле mcp константное, единственное, что изменяться это userId & workspaceId в параметрах заголовка

---

Обнови дизайн главной страницы в соответствие с правками от маркетолога:

1. для какой ЦА делаем продукт? Точно ли она понимает ноушн и обсидиан?
   Боюсь, что если ограничиться только теми, кто понимает, ЦА будет узковата, а значит тяжело будет дотянуться и упороться в планировании каналов распространения, ну и быть готовым к небольшому потоку клиентов
   Собери всю информацию о том, для какой целевой аудитории подходит этот продукт и выведи
2. слоган интересный, мне нра, но с тз дизайна - нечитаемо вообще
3. Оч много терминологии, пересекается с п.1. + англицизмы глаз режут. Переведи на понятный русский язык. Легче переписать
4. life preview не читаемо, сделай чтобы больше било в цель продукта
5. Далее я не понимаю ценность для клиента: зачем ему альтернатива? в чём его боль? Лучше в заг выносить именно решение конкретной боли.
   Текста для зага много и слова оч абстрактные
6. Определить ЦА и делать описание продукта под неё. Чем специфичнее терминология, тем уже ЦА
7. Понять боль ЦА и предлагать продукт в качестве решения этой боли через его УТП и через выгоду для клиента. Например, вместо “увеличивайте лояльность ваших клиентов” лучше написать “получайте повторные продажи с большим средним чеком”. Обе фразы про одно и тоже, но в первом случае что-то абстрактное, во втором - какую конкретно выгоду, я как клиент, получу. Или вместо “запускайте больше акций с нашей программой лояльности” лучше писать “привлекайте ещё больше клиентов”. Акции для продавца - это невыгодно, а вот больше клиентов - это то, что нужно каждому
8. Чем проще описание и чем меньше текста, тем больше вероятность, что это прочитают вообще
9. По структуре. В пользуются следующей структурой ленда: сначала блок с УТП, потом краткое описание, что даёт продукт, затем цена и что нужно, чтобы подключить. Лишнее убирай: чем больше текста, тем меньше желания читать

---

Делаем новый раздел на странице workspaces/{workspaceId}/settings/files после раздела AI агент "Файлы", в которм выводить загруженные в workspace файлы

- выводить списоком всю информацию о файлах
  - название файла
  - кто загрузил файл
  - расширение файла (к расширению предусмотреть иконки, если такие есть)
  - размер файла в Мегабайтах
  - Статус
  - количество скачиваний
- добавить контрол скачивания и удаления файла, если пользователь загрузил файл, при нажатии выводить модальное окно удаления файла
- сделать фильтры в виде чипов поиска
  - поиск по названию файла
  - По пользователю, при нажатии на чип появляется меню с выбором пользователей, которые загружали файлы
- сделать пагинацию, выводить по 20 файлов за один раз

---

Приступаем к реализации RAG

1. У нас есть индексирование страниц, которое работает в apps/engines
   Нужно добавить в метадату к кусочкам сохраняемым в qdrant добавить поле content - нормализованный текст и title = page.title

какие данные еще стоит добавить в metadata? 2. При отправке запроса в сервисе apps/web нужно сделать поиск по векторной базе знаний и добавлять в запрос параметры поле rag

```
{
  "threadId": "<chat.id>",
  "model": {
    "provider": "<workspaceDefaultModel.provider.slug>",
    "name": "<workspaceDefaultModel.slug>",
    "connection": "<workspaceDefaultModel.provider.connection>",
    "settings": {
      "temperature": "<workspaceAiSettings.temperature>",
      "topP": "<workspaceAiSettings.topP>"
    }
  },
  "systemPrompt": "<workspaceAiSettings.systemPrompt or empty string>",
  "rag": { # new field in query
    "documents": [
      { id: "<metadata.pageId>", "title": "<metadata.title>", "content": "<metadata.content>" }
    ]
  }
  "mcp": {
    "servers": [{
      "name": "AnyNote MCP Server",
      "url": "http://localhost:8090/api/mcp",
      "headers": {
        "Content-Type": "application/json",
        "Accept": "application/json, text/event-stream",
        "X-User-Id": "<session.user.id>",
        "x-Workspace-Id": "<chat.workspaceId>"
      },
      "retries": 3,
      "verify": false
    }]
  },
  "instruction": {
    "format": "markdown",
    "language": "ru",
    "citationsRequired": true
  },
  "query": "<user text>"
}
```

3. Нужно поправить в промпт формирование rag agents/apps/chat/templates/default.j2, чтобы LLM понимало и могло вызывать тулинг по информации для страниц, может быть стоит изменить шаблон промпта на jinja2, чтобы более качественнее отвечать
4. реализуй проверку и проведи тесты на playwright что rag работает

---

Необходимо отображать чат Messages и работу tools при общении на странице chats /workspaces/{workspaceId}/chats/{chatId}

- Сейчас тулинг отображается только в момент стриминга при генерации текста, необходимо сделать, чтобы он всегда показывался
- Давай поменяем структуру ChatMesagges в packages/db
  - удали поле content
  - добавим поле parts типа JSONB которое будет содеражть части сообщения для рендеринга их на странице чатов с типами
    - text
    - tools - для тулинга из генерации LLM
    - attacments - для файлов
  - удалим таблицу ChatMessageFile, информация о файлах будет записываться в ChatMessage.parts
- в связи с новой структурой поправим вывод сообщений и их сохранение на apps/web

---

Сервис apps/engines занимается индексаций страниц, которые имеют Page.PageType = 'TEXT', индексация происходит через OLLAMA модель
однако при его работае возникают проблемы, связанные с тем, что текста слишком длинные и их нужно разбивать на части

1. Исключи из индексации блоки с типами

- все заголовки
- изображения
- файлы
- скрытый текст

2. Добавить в сервси apps/agents библиотеку для разделения большого текста на кусочки langchain_text_splitters и ответе о нормализации текста расширить контракт и выозвращать не нормализованный текст, а возвращать массив кусочков нормализованного текста chunk_size=500 overlap=100 символов
3. для каждого кусочка сделать векторизацию

---

Метаданные, которые выглядят следующим образом в qdrant и которые туда сохраняются

```json
{
  "pageId": "0296228a-62e1-4c81-9744-0655e45af5bd",
  "workspaceId": "2991d511-0114-45b7-bd3b-8c9469f09b56",
  "chunkIndex": 112,
  "title": "Архитектура Backend на python",
  "content": "responseschema basemodel схема ответ настройка генерирование camelcase snake case camelcase model config configdict alias generator aliasgenerator serialization alias to camel remoteresponseschema requestschema remoterequestschema responseschema",
  "pageType": "TEXT",
  "createdById": "019db0f8-5146-7740-8476-9b5802c2af08",
  "createdAt": "2026-04-24T05:44:21.587Z",
  "updatedAt": "2026-04-24T05:47:37.417Z"
}
```

Давай приведем все остальные контракты к этим параметрам в

1. apps/web/src/lib/chat/rag-search.ts
2. apps/agents/agents/apps/chat/schemas.py
3. apps/agents/agents/apps/chat/templates/default.j2

прогони тесты, что это работает

---

Нужно переделать механизм получения и индексации страниц

1. Сохранять контент страницы при работе в многопользовательском режиме через сервис apps/yjs

- page.content должен загружаться сразу при открытии страницы, а не через yjs через socket, через yjs должен идти только обмен на изменения
- страницы с типом excalidraw должны сохранять свое состояние в page.content и отдавать его при загрузке страницы
- при любом изменении нужно страницы через yjs должна ставиться задача на обработку по transactional outbox в таблицу OutboxEvents
- при любом изменении нужно страницы через yjs должна ставиться задача на обработку по transactional outbox в таблицу OutboxEvents
- должен быть механизм дедупликации, если разные пользователи меняют страницу, должна быть одна запись
  по aggregate_type, aggregate_id, workspace_id со статусом panging
- запись должна забираться через 5 минут после created_at, чтобы не было тротлинга

2. В сервисе apps/engins изменить алгоритм OutboxEvents где status = PENDING и created_at + 5 min > now() чанками по 10 страниц за один раз

- поле page.content пробегаемся по массиву content первого уровня внутри документа, из которых формируется блок, который состоит из двух полей
  - blockNumber - порядковый номер блока (нужно для подсветки блока)
  - content - формируется как контект блока, а если блок имеет дочерние элементы, то проходимся рекурсивно и текст из дочерних блоков просто конкатенируем друг с другом
- пустые блоки
- блоки с типом header, hidder, image, files не учитываем и пропускаем, как на первом уровне, так при проходе в дочерних элементов
- получившийся результат отправляем в сервис apps/agents post запросом на /vectorization
- работу с embedding из сервиса apps/engines удалить, лишние зависимости также удалить
- модуль search удалить
- в модуле apps/engines остается только mcp и крон по отпраке контента в apps/agents

3. В сервисе apps/agents создать API для векторизации страниц в приложении apps/agents/agents/apps/processing
   - сделать интеграцию с qdrant добавив библиотеку langchain-qdrant
   - сделать API post запрос /vectorization для векторизации страниц страниц, у которого будет следующее API

```json
{
  "pageId": "<page.id>",
  "workspaceId": "<page.workspaceId>",
  "title": "<page.title>",
  "pageType": "<page.page_type>",
  "contents": [{ "blockNumber": 0, "content": "<text>" }]
}
```

    blockNumber - номер блока на странице, content - текст присланный из сервиса apps/engines по API
    После этого алгоритм обработки контента такой
    - для каждого content в contents
      1. разбить на чанки с помощью langchain-text-splitters RecursiveCharacterTextSplitter chunk_size=500 chunk_overlap=100
        для каждого чанка
          - привести к нижнму регистру
          - удалить знаки пунктуации
          - токенизацию
          - лемматизацию
          - удалить стопслова
          - сделать векторизацию с помощью ollama nomic-embed-text (VerctirizationRepository)
        в качестве matadata записать следующее

```json
{
  "pageId": "<page.id>",
  "workspaceId": "<page.workspaceId>",
  "title": "<page.title>",
  "pageType": "<page.page_type>",
  "blockNumber": "<contents[number].blockNumber>",
  "content": "исходный чанк после сплитинга"
}
```

workspaceId и pageId для генерацииmd ссылки [<metadata.title>](/workspaces/<workspaceId>/pages/<pageId>#<blockNumber>)
полученные чанки сохранить в qdrant VectorStoreRepository
инжекция зависимостей на примере apps/agents/agents/apps/chat/depends.py - для векторизациии используй langchain-ollama OLLAMA и модель nomic-embed-text

- коллекция в qdrant должна назваться pages
- добавить интеграцию с vector store на основе quadrant в LangGraph apps/agents/agents/apps/chat/services/graph.py используя библиотеку langchain-qdrant
  при запросе необходимо делать поиск 5 документов, делать дедубликацию документов по pageId, blockNumber и подмешивать их в запрос llm
  внести соответствующие правки в default.j2 шаблон, если раздел rag не нужен
  - для quadrant и для OLLAMA добавь в settings настрйки apps/agents/agents/settings.py
    CoreServiceSettingsSchema
- исправить шаблон если это необходимо
  для qdrant предусмотреть авторизацию по токену BearerTokenAuthSchema
  - rest запрос post /normalize удалить

4. Провести с помощью playwright тесты, убедиться, что в ответе есть ссылки на страницы с указанием блока, на который мы ссылаемся во время запроса

---

Необходимо добавить в запрос отправку истории сообщений message в схему QueryRequestSchema

1. Необходимо добавить в сервисе apps/web добавлении историй чата и передачи параметров messages

- если чат не имеет родителя chat.parentId === null, messages собираем следующим образом
  - первое сообщение в чате
  - последних 10 сообщейни исключая первое
    это необходимо для сохранения контекста
- если чат имеет родительский чат, messages собираем следующим образом
  - рекурсивно проходимся по родителям их которых берем первое сообщение и последних 4 в каждом чате, например для chat.id = 3
    - chat.id = 1 и chat.parantId == null
      - первое сообщение
      - 4 последний сообщений
    - chat.id = 2 и chat.parentId == 1
      - первое сообщение
      - 4 последний сообщений
    - chat.id = 3 и chat.parentId == 2
      - первое сообщение
      - 10 последних сообщений
- сообщения добавить в запрос для генерации текста apps/web/src/lib/chat/agents-payload.ts

2. Когда ответ еще не пришел на странице чата вместо пустой обертки сообщения сделать раз в секунду смену следующих фраз

- Загрузка
- Вычисления
- Преобразование
- Литье
  когда ответ начинает стримиться, в такмо случае показваем как и сейчас

3. Провести с помощью playwright тесты, убедиться, что в ответе есть ссылки на страницы с указанием блока, на который мы ссылаемся во время запроса

---

Давай примением некоторые изменения по отображению page.pageType === 'TEXT'

1. Чат делает ссылки на страницы (title)[/workspaces/{workspaceId}/pages/{pageId}#85], где цифра после # - 85 это ссылка на блок первого уровня в tiptap, при переходе на такую страницу нужно

- проскролить до этого блока, чтобы он стал посреди страницы
- на 3 секунды подсветить этот блок светложелтым фоном, явно отмечая, что мы выделили его

2. сейчас контент страницы page.pageType === 'TEXT' отображается с задержкой и нет лоадера загрузки контента, необходимо сделать скелетон по загрузке страницы

---

Работа индексатора происходит не совсем корректно, необходимо сделать следующее

1. выяснить как работает механизм индексации
2. необходимо сопоставить с алгоритмом и поправить
3. алгоритм индексации должен быть следующий

- раз в пять минут запускается cron операция apps/engines/src/apps/indexer/cron/vectorization-cron.service.ts, которая читает из таблицы OutboxEvents строки следующим образом
  - выбираем последнюю запись order by created desc limit 1 по группировке aggregate_type, aggregate_id, workspace_id
- если событие event_type = 'page.deleted', то из qdrant просто удаляем векторы
- если событие event_type = 'page.upserted', то удаляем векторы для этой страницы и делаем векторизацию, по алгоритму который сейчас
- все остальные страницы по aggregate_type, aggregate_id, workspace_id удаляем или ставим статус DONE, чтобы в следующий раз эти записи не обрабатывась снова

---

Давай разберемся с тарифными планами, доступно 3 вида подписов

1. Сделаем три тарифных плана
   - Персональный - тарифный план по умолчанию и при регистрации пользователя для личного пользования
     - Доступно 1 рабочее пространство
     - Чаты не доступны - раздел /workspaces/{workspaceId}/chats раздел чатов скрыт
     - страницы не индексируются в векторной базе знаний qdrant
     - раздел участников скрыт /workspaces/{workspaceId}/settings/members
     - раздел AI агентов скрыт /workspaces/{workspaceId}/settings/ai
   - ПРО
     - доступно 3 рабочих пространства
     - Чаты доступны
     - страницы индексируются в векторной базе qdrant
     - разрешено добавлять до 5 участников /workspaces/{workspaceId}/settings/members
     - на странице AI агентов можно выбрать GigaChat-2 или Gigachat-2-Pro
   - МАКС
     - доступно неограниченное количество пространств
     - Чаты доступны
     - страницы индексируются в векторной базе qdrant
     - разрешено добавлять до 100 участников /workspaces/{workspaceId}/settings/members
     - на странице AI агентов можно выбрать GigaChat-2, Gigachat-2-Pro, Gigachat-2-Max
     - добавление собственных MCP серверов
     - приоритетный доступ к поддержке
     - доступ к пространству разработчиков и возможность влиять на беклог продукта, голосовать за задачи
   - Собственная инфраструктура
     - обсуждается персонально

для тарифов ПРО и МАКС на страницах workspaces в левом сайдбазе где выводиться тарифных план сделать их выделенным зеленым тегом 2. Стоимость тарифных планов

- Персональный - бесплатно, выдается сразу при регистрации
- ПРО
  - 1 месяц - 150 рублей
  - 1 год - 1000 рублей
- MAX
  - 1 месяц - 1500 рублей
  - 1 год - 12 000 рублей

3. Необходимо организовать оплату через Юкассу

- изучи документацию со страниц
  - https://yookassa.ru/developers/payment-acceptance/getting-started/quick-start
  - https://yookassa.ru/developers/payment-acceptance/getting-started/payment-process
  - https://yookassa.ru/developers/payment-acceptance/getting-started/payment-methods
  - https://yookassa.ru/developers/payment-acceptance/getting-started/selecting-integration-scenario
  - в виду того, что у нас подписочная модель, мы должны списывать деньги за пользование
    - https://yookassa.ru/developers/payment-acceptance/scenario-extensions/recurring-payments/basics
    - https://yookassa.ru/developers/payment-acceptance/scenario-extensions/recurring-payments/save-payment-method/save-during-payment
- для клиентов сделай крон операцию в 12 ночи в день, когда подписка заканчивается, делай автоматическое продление
  - https://yookassa.ru/developers/payment-acceptance/scenario-extensions/recurring-payments/pay-with-saved
- также предусмотри cli команды о полном возврате денег в apps/engines
  - https://yookassa.ru/developers/payment-acceptance/after-the-payment/refunds

4. поправь информацию о тарифах на глвное странице и на странице покупки
5. При отказе от подписки, подписка не пропадает, она действует, просто новая подписка не возобновляется, предусмотри флаг в таблице заказов

---

Необходимо расширить работу с аутентификацией и регистрацией пользователей

1. Добавь возможность отправлять почту

- добавь в инфраструктуру mailhog docker контейнер для проверки отправки почты через smtp
- создай пакет @repo/mail в котором будут представлены шаблоны следующих писем
  - Подтверждение email - для завершения регистрации: ссылка/код подтверждения, срок действия, что делать, если пользователь не регистрировался
  - Добро пожаловать - после успешной регистрации или подтверждения email: кратко о продукте, следующий шаг, ссылка на вход
  - Сброс пароля - Ссылка/код для установки нового пароля, срок действия, предупреждение “если это были не вы — проигнорируйте письмо”
  - Пароль изменён - Уведомление о том, что пароль был успешно изменён, и контакт поддержки при подозрении на взлом
  - Email изменён - Подтверждение смены email, желательно отправлять и на старый, и на новый адрес
  - Новый вход в аккаунт - Уведомление о входе с нового устройства, IP, браузера или локации
  - Блокировка или подозрительная активность - Например: много неудачных попыток входа, временная блокировка, подозрительный вход.
  - Приглашение пользователя - Если в системе есть команды, организации или роли
  - Удаление аккаунта - Подтверждение запроса на удаление и отдельное письмо после удаления, если это предусмотрено
    заполни шаблоны кратко и по делу, без стилизации

2. Измени страницу входа /sign-in необходимо

- Сверху вместо Авторизация по строкам
  - Иконка AnyNote
  - Вход в учетную запись
- вертикальная черта
- Вход с помощью Google
- вертикальаня черта
- Поле ввода Email
- Поле ввода пароля
- Запомнить меня слева и справа Забыли пароль? ссылка которая ведет на восстановление пароля
- Кнопка Вход
- вертикальная черта
- Новый пользователь? Регистрация и ссылка на страницу регистрации

3. Измени страницу регистрации /sign-up

- Сверху вместо Регистрация по строкам
  - Иконка AnyNote
  - Регистрация
- Поле ввода Email
- Поле ввода Фамилия
- Поле ввода Имя
- Пароль
- Повторите пароль
- KeyboardDoubleArrowLeftIcon Назад ко входу
- Кнопка Регистрация

4. Добавь регистрацию через Google при нажатии на странице /sign-in Войти через Google, сделать имплементацию через @repo/auth

- если при аутентификации пользователь создался новый, то отправить email "Добро пожаловать"

5. Добавить странице восстановить пароля /reset-credentials

- Иконка AnyNote
- Забыли пароль
- Поля ввода Email
- KeyboardDoubleArrowLeftIcon Назад ко входу
- Подтвердить
- вертиальная черта
- Введите Ваш E-mail и мы вышлем Вам инструкции по получению нового пароля

При нажатии на подтвердить отправляется письмо со ссылкой на сброс пароля по шаблону сброс пароля, ссылка должна быть действительна 3 часа и быть одноразовой
После успешной смены пароля необходимо сделать переадресацию на /sign-in 6. Добавить страницу подтверждения Email

- при запросе подтверждения Email нужно отправить письмо со ссылкой
- ссылка действует 3 часа
- при переходе на ссылку Email подтверждается

7. Добавить reCapcha на страницах

- /sign-in - при нажатии на кнопку Авторизация
- /sign-up - при нажатии на кнопку Регистрация
- /reset-credentials - при нажатии на Подтвердить

---

Необходим расширить функционал страницы с типом page.pageType === 'GENOGRAM':

Большие элементы

- большой квадрат
- большой круг
- большой квадрат с квадратом внутри
- большой круг с кругом внутри
  Маленькие элементы
- маленький квадрат
- маленький круг
- маленький крест с буквой "А" в правом верхнем углу
- маленький крест с буквой "В" в правом верхнем углу
  Крест изображается пересекающимися диагональными линиями под прямым углом

Подписи к элементам:

- текст не наслаивается на элементы
- текст располагается справа от больших элементов
- текст располагается внизу под маленькими элементами
- выводится только важная информация

На уровне элемента "Владелец генограммы" по правому краю отображаеся подпись "Дата создания генограммы" с указанием даты

- все даты в генограмме в формате <день> <название месяца> <год>

Графа "Данные владельца":

- Фамилия
- Имя
- Отчество
- Пол: Мужской/Женский
  - компонент ButtonGroup
- Дата рождения

Графа "Данные": - Фамилия - Имя - Отчество - Пол: Мужской/Женский - компонент ButtonGroup - Дата рождения / Приблизительный возраст: - компонент ButtonGroup - если выбран "Приблизительный возраст", отображается количество лет или диапазон в формате <число-число> - Жив/Умер/Неизвестно: - компонент ButtonGroup - если выбран статус "Жив", возраст считается автоматически на дату создания генограммы - если выбран статус "Умер": - появляется поле "Дата смерти" - возраст считается автоматически по дате смерти и больше не обновляется - если выбран статус "Неизвестно", то справа от элемента в подпись добавить вопросительный знак

Графа "Брак/Отношения": - компонент ButtonGroup - если выбран статус "Брак", появляются дополнительные поля: - дата свадьбы - [ ] брак расторгнут: - компонент CheckBox - если [ ], дополнительных полей не появляется - если [ Х ], появляется дополнительное поле:

- дата развода - если выбран статус "Отношения", появляются дополнительные поля: - дата начала - [ ] отношения закончены: - компонент CheckBox - если [ ], дополнительных полей не появляется - если [ Х ], появляется дополнительное поле: - дата завершения

Родители: - на схеме располагаются элементы на одном уровне: - большой квадрат слева - большой круг справа - от элементов вертикально вниз выходит линии, которые соединяются под прямым углом горизонтальной линией - вся эта конструкция называется родительской линией связи - виды родительской линии связи: - если выбран статус "Брак", то сплошная основная - если выбран статус "Отношения", то штриховая - если нажата кнопка "Добавить родителей", то сплошная основная - если есть дети, то от родительской линии связи идут под прямым углом сплошные основные линии к элементам детей

Кнопка "Сохранить" сохраняет изменения формы.
Кнопка "Отменить" не сохраняет изменения формы.

При нажатии на элемент выпадает меню: - компонент BasicMenubar - если элемент маленький, то "Редактировать данные" - если элемент большой квадрат, большой круг: - "Редактировать данные" - "Добавить партнера" - если у большого элемента нет родителей, то "Добавить родителей" - если элемент большой квадрат с квадратом внутри, большой круг с кругом внутри: - "Редактировать данные владельца" - "Добавить партнера"

Кнопка "Редактировать данные": - поля графы "Данные" - кнопка "Отменить" - кнопка "Сохранить"

Кнопка "Редактировать данные владельца": - поля графы "Данные владельца" - кнопка "Отменить" - кнопка "Сохранить"

Кнопка "Добавить партнера": - поля графы "Данные" - поля графы "Брак/Отношения" - кнопка "Отменить" - кнопка "Сохранить"

Кнопка "Добавить родителей": - на схеме добавляются элементы родителей для выбранного элемента-ребенка, соединенные по установленным правилам

При нажатии на линию связи выпадает меню: - компонент BasicMenubar - "Редактировать связь" - "Добавить детей"

Кнопка "Редактировать связь": - поля графы "Брак/Отношения" - кнопка "Добавить детей" - кнопка "Отменить" - кнопка "Сохранить"

Кнопка "Добавить детей": - "Укажите количество детей" - <число> - если у родителей уже есть ребенок: - первым номером по умолчанию существующий ребенок с данными ФИО - в строке уже существующего ребенка слева кнопка для перемещения его положения между другими детьми - появляется <число - 1> строк с порядковым номером и полем "ребенок/выкидыш/аборт" - если у родителей на схеме нет ребенка, то появляется <число> строк с порядковым номером и полем "ребенок/выкидыш/аборт" - кнопка "Отменить" - кнопка "Сохранить" - на схеме дети добавляются маленькими элементами

Графа "ребенок/выкидыш/аборт": - компонент ButtonGroup - если выбран статус "ребенок", появляются поля графы "Данные" - если выбран статус "выкидыш" или "аборт", появляется поле "Дата"

Начало работы: - форма графы "Данные владельца" - кнопка "Отменить" - кнопка "Создать генограмму"

Кнопка "Создать генограмму"

- на схеме добавляется элемент владельца генограммы:
  - если выбран пол "Мужской", то большой квадрат с квадратом внутри
  - если выбран пол "Женский", то большой круг с кругом внутри
- на схеме добавляются элементы родителей, соединенные по установленным правилам
- подписи отображаются справа от элементов

---

Давай добавим уточнения.

Элементы с меткой "Умер":

- квадрат с крестом внутри по диагонали - мужчина "умер"
- круг с крестом внутри по диагонали, вписанный в окружность - женщина "умер"

Если выбран статус "Умер" в графе "Данные":

- добавить компонент CheckBox с подписью "Трагически":
  - если [ ], дополнительных полей не появляется
  - если [ Х ], элемент с меткой "Умер"
- если возраст человека до 65 лет, то элемент с меткой "Умер"

## Дата в формате <день> <название месяца> <год> может быть заполнена частично.

Давай уточним пункт 5.6 по линии связи между родителями, пункт про развод с наклонными линиями поверх линии связи:

- наклонные линии по умолчанию ставятся в середине
- делаем эти линии кнопкой как ползунок, чтобы после ее появления можно было перемещать наклонные линии вдоль линии связи в пределах линии связи

---

Давай добавим правило добавления партнера при нажатии кнопки "Добавить партнера"

- если при добавлении партнера выбран пол "Мужской", то на схеме располагаем элемент слева на одной линии иерархии
- если при добавлении партнера выбран пол "Женский", то на схеме располагаем элемент справа на одной линии иерархии
- добавить поле "Укажите количество партнеров":
  - если введено число 1, то внутри элемента-партнера число не пишем
  - если введено число больше 1, то внутри элемента-партнера пишем его порядковый номер:
    - элементы-партнеры располагаются согласно порядковому номеру с соблюдением иерархии старшинства слева направо
    - при редактировании данных дабавить дополнительное поле "Порядковый номер партнера":
      - если пользователь генограммы меняет значение числа, то на схеме меняется расположение элемента согласно иерархии старшинства

И добавим поле "Порядковый номер ребенка" в кнопке "Добавить детей"

- на схеме элементы без изменения
- при редактировании данных ребенка поле "Порядковый номер ребенка" можно изменять:
  - если пользователь генограммы меняет значение числа, то на схеме меняется расположение элемента согласно иерархии старшинства

---

На текущий момент у нас модель embeddings одна и это ollama, однако, с помощью других моделей, которые поддерживают embeddings это сделать также можно

1. Добавить в AiModel флаг embeddings, который означает что модель может делать векторизацию
2. Добавить в WorkspaceAiSettings embeddings_model_id, которая будет отвечать за то, какая модель будет векторизировать
3. На странице с настройками AI агента /workspaces/{workspaceId}/settings/ai необходимо добавить раздел Векторизация по аналогии с AI моделью, в котором будем выбираться векторизация
4. Если для workspace векторизатор не указан, значит при индексации пропускаем
5. Если мы меняем модель векторизации

- с одной модели на другую - происходит полное удаление из qdrant всех векторов для этого пространства и они все ставятся на веторизацию заново
- если мы просто удаляем модель векторизации, это означает, что мы просто удаляем все векторы из qdrant
- предусмотреть в apps/agents рест запрос для удаления из qdrant всех векторов

6. Если векторизатор не выбран, то при генерации ответа мы не формиурем ответ и фазу поиска с по базе qdrant пропускаем

---

Поправь отрисовку родительской линии связи - от элементов родителей большого квадрата слева и большого круга справа вертикально вниз выходит линии, которые соединяются под прямым углом горизонтальной линией

---

Внеси правки в отрисовку линий:

- П-образная скобка родительской связи должна выходить посередине из нижней части элементов родителей, оцентрована по вертикали
- Из П-образной скобки родительской линии связи должны выходить прямые параллельные сплошные линии к элементам-детям без П-образной скобки сплошной линии
- Линии к детям параллельно друг другу
- П-образная скобка родительской связи шире, чем количество детей
- Если увеличивается число детей, то элементы-родители должны стать дальше друг от друга по горизонтали

Внеси правки в подписи к элементам:

- большой элемент слева, его подпись справа
- текст подписей не должен наслаиваться на элементы

---

Внеси правку в подпись к большим элементам

- текст должен быть весь виден
- элемент не закрывает текст
- если элемент наслаивается на текст, то смещаем элемент вправо, чтобы сделать текст видимым

Если выбран пункт "Отношения закончены":

- две наклонные черты на родительской линии связи
- правила поведения такие же, как при "Брак расторгнут"

Поправить отрисовку элемента выкидыша и аборта:

- крест диагональный
- размер креста совпадает с маленькими элементами

---

Внеси правки:

- в отрисовку элемента аборта и выкидыша:
  - в правом вехнем углу креста сделать на несколько пикселей выше подписи "А" и "В"
  - линия креста не должна заходить на букву
- в меню "Редактирование связи" при выборе "Отношения":
  - если выбран пункт [ x ] "Отношения закончены":
    - сохранять выбор
    - на схеме добавить две наклонные черты на родительской линии свзяи
- добавление нового партнера:
  - каждый новый партнер добавляет свою П-образную скобку родительской связи
  - из элемента, которому добавляем партнера, выходят параллельные прямые линии в каждой родительской линии связи
  - если от большого квадрата мужчины добавляется новый партнер женщина большой круг:
    - новый элемент большой круг располагается справа от предыдущего партнера женщины большой круг
  - если от большого круга женщины добавляется новый партнер мужчина большой квадрат:
    - новый большой квадрат располагается слева от большого круга женщины И справа от предыдущего партнера мужчины большой квадрат

---

Внеси правки:

- добавление нового партнера:
  - если партнеров больше одного, их горизонтальные родительские линии связи должны быть параллельными
  - если от большого квадрата мужчины N партнеров женщин большой круг:
    - новый элемент N+1 женщина большой круг добавляется справа от партнера N женщины большой круг
- отрисовка элементов аборта и выкидыша:
  - линии креста одинакового размера
  - в правом вехнем углу креста сделать на несколько пикселей выше подписи "А" и "В"
  - линия креста не должна заходить на букву
  - крест одного размера с маленькими элементами

---

Давай расширим функицонал при нажатии на пользователя в низу в левом сайдбаре на старнцие /workspaces/{workspaceId}
Сейчас там меню с Мой профиль, настройки и выйти, сделаем меню более рабочим

- PersonIcon Профиль - ведет на /profile
- SettingsIcon Настройки - ведет на /settings
- вертикальная черта
- ArrowCircleUp Обновить план /pricing, показывать если текущий план не равер плану макс
- LogoutIcon ведет /sign-out

---

Давай сделаем несколько корректирующих изменений

1. На тарифе макс я могу создаать много пространств, давай на странице /profile слева от РАБОТЧИЕ ПРОСТРАНСТВА добавим AddIcon которая будет вести на /workspaces/new
2. Убери в левом сайдбаре на странице /workspaces тек тариф, который находится под названием пространства
3. Если у меня больше чем одно пространство, то при нажатии на название пространства в левом сайдбаре выводи Menu c выбором, в меню иконка пространства и название
4. В левом сайдбаре внизу на странице /workspaces/{workspaceId}, где выподится аватар ИМЯ и фамилия внизу вместо почты сделай тег тарифа
5. В левом сайдбаре внизу на странице /workspaces/{workspaceId}, когда мы нажимаем на ФИО выводиться менню и там добавь почту как subtitle

---

Основываясь на стадии лучших практик сделать CI/CD pipeline для развертывания приложения
Репозиторий проекта на github git@github.com:AnyNoteInc/AnyNote.git
CI/CD pipeline должен состоять из следующих шагов

- когда мы мерджим в дефолтную ветку
  - статия линтинга
  - стадия тестов
  - подготовка нового релиза с помощью semantic-release (формирование тега и changelogs)
- при формировании нового тега, выхода нового релиза, который сформирован на основе semantic release
  - стадия сборки докер образов
    - agents
    - engines
    - web
    - yjs
  - стадия деплоя, однако на эту стадию пока сделай мок echo 'deploy'
- когда мы делаем PR в дефолтную ветку
  - стадия лининга
  - стадия тестов
- сборку образа сделай в регистр github
- проверь, что все контейнеры запускаются и работают

---

Нам необходимо создать дирректорию deploy и туда положить compose.yml файл со всеми конфигурациями для того, чтобы приложение запустить в продакшене
Сайт будет распологаться на домене https://anynote.ru

1. Создай deploy/compose.yml который будет содержать следующие контейнеры, контейнеры возьми из

- traefik - API gateway который проксирует запросы до других контейнеров приложений, будет являться входной точкой для приложений И
  - yjs - https://anynote.ru/ws/_ -> https://yjs:1234/_
  - web - https://anynote.ru/_ -> https://web:8000/_
  - также для traefik добавь возможность формирования сертификатов через lets encrypt, пердусмотри все необходимые пути для регистрации сертификатов
  - для traefik подключи админку на порту 8080 с логином и паролем
  - добавь плагин rate limiter и защиту от DDOS атак
- web - сервис apps/web
- yjs - сервис apps/yjs
- engines - сервис apps/engines
- agents - сервис apps/agents
- postgres
- minio
- qdrant
  контейнеры, которые сервисные, их нужно взять с github

2. Приготовь deploy/.env.example - файл окружения для примера, чтобы файл .env сделать на сервере
3. Проверь, что все работает при запуске docker compose up -d, именно так мы и будем запускать
4. env файлы я записал в variables github и они следующие по скиску

- DEPLOY_KEY_PRIVATE - приватный ключ для авторизации по ssh
- DEPLOY_HOST - хост для авторизации на удаленном сервере по shh
- DEPLOY_PORT - порт для авторизации на удаленном сервере по ssh
- DEPLOY_USER - пользователь для авторизации на удаленном сервере по ssh

- NEXT_PUBLIC_BASE_UR=https://anynote.ru
- BETTER_AUTH_URL=https://anynote.ru
- NEXT_PUBLIC_YJS_URL=https://anynote.ru/ws

- NEXT_PUBLIC_RECAPTCHA_SITE_KEY
- RECAPTCHA_SECRET_KEY

- GOOGLE_CLIENT_ID
- GOOGLE_CLIENT_SECRET

- YOOKASSA_SHOP_ID
- YOOKASSA_SECRET_KEY

- SMTP_HOST
- SMTP_PORT
- SMTP_SECURE
- SMTP_USER
- SMTP_PASSWORD

- S3_ENDPOINT=http://minio:9000
- S3_REGION=us-east-1
- S3_ACCESS_KEY=admin
- S3_SECRET_KEY=nOlxtgQDnQhcPeRHrwaSoB6Zll8TYpQW
- S3_BUCKET=storage
- S3_FORCE_PATH_STYLE=true

- QDRANT\_\_HOST=http://qdrant:6333
- QDRANT**AUTH**TYPE=bearer_token
- QDRANT**AUTH**BEARER_TOKEN=gasVRZyq0spAFDVAXJkhxcbG2czDfh2V
- QDRANT\_\_COLLECTION_NAME=pages

- AGENTS_SERVICE_URL=http://agents:8080
- ENGINES_URL=http://engines
- ENGINES_PORT=8082

5. По итогу на стадии deploy долнжо происходить конструирование compose.yaml и .env на удаленном сервере и запуск docker compose up -d
   .env необходимо формировать на лету из шаблона

---

Необходимо оформить документы на сайте

1. Документы находятся в папке docs/terms

- docs/terms/ConsentToProcessing.md - Согласие на обработку персональных данных
- docs/terms/Information.md - Информация о самозанятом
- docs/terms/PrivacyPolicy.md - Политика в отношении обработки персональных данных
- docs/terms/PublicOffer.md - ПУБЛИЧНАЯ ОФЕРТА
- docs/terms/UserAgreement.md - Пользовательское соглашение

2. Необходмио создать группу путей terms (Условия) и там сделать ссылки на документы выше и внутри использовать рендер markdown файлов

- для рендера md файлов необходимо использовать пакеты @next/mdx @mdx-js/loader @mdx-js/react @types/mdx

3. На всем сайте где встречаются ссылки нужно поменять на новые
4. При регистрации нужно ставить галочку по примеру
   Я принимаю:
   - пользовательское соглашение
   - политику обработки персональных данных
   - оферту на оказание услуг по осуществлению переводов
5. В подвале сделать раздел Юридические документы, где списком перечислить документы выше

После всех работ сделать следующие действия

- с помощью плагина playwright прогнать все тесты по загрукзе картинок и файлов
- проверить команды
  - pnpm run link
  - pnpm run format
  - pnpm run check-types

---

Давай переделаем отправку email сообщений в некоторых местах,
необходимо отправлять email в потоке синхронно на основе @repo/mail

- /sign-up - во время регистрации, если email не отправился, значит пользователя не регистрируем
- /reset-credentials - при попытки восстановления пароля, если email не отправился то запись в бд не делаем и пользователю показываем ошибку
- /settings/general - если email не подтвержден, значит тег должен быть активен, а при нажатии должно синхронно отправляться email с подтверждением пароля

- проверить команды после работы
  - pnpm run link
  - pnpm run format
  - pnpm run check-types

После всех работ сделать следующие действия

1. Сделай PR и дождись завершения workflows и sonarqube
2. Поправь если появились ошибки в sonarqube или упали тесты
3. После этого вмерджи
4. Проверь, чтобы деплой дошел до конца
5. С помощью плагина playwright прогнать, что так работает после деплой


---

Давай сделаем еще некоторое количество улучшений
1. на странице редактора /workspaces/{workspaceId}/pages/{pageId} при экспорте в pdf нужно добавить стандартные отступы для листов A4
2. когда я перехожу на /workspaces/{workspaceId} должно открываться первая старница, если она есть, сейчас это не так
3. В редакторе на странице Page.PageType == 'TEXT' в редакторе tiptap меняю цвет фона строчки и меняется цвет всей ширины строки. сделай выделение только по ширине текста
4. Необходимо добавить компонент /date который открывает DatePicker и ставит текущую дату, также добавь /datetime который ставит текущую дату и время
5. Фон в светлой теме хочется светлее. Чтобы был выше контраст

каждую задачу сделай в отдельном субагенте

и проверь с помощью playwright

---

Необходимо сменить механизм отправки email уведомлений
Механизмы отправки сообдений находятся в @repo/mail
Сейчас отправка осуществялеться через stmp механизм
Необходимо заменить отправку по stmp на отправку через провайдера sendsay
1. Удали все пакеты, которые связаны с отправкой сообщений по smtp
2. Поставь sendsay-api для отправки сообщений
3. Не меняя пользовательское api @repo/main замени отправку сообщений на sendsay
4. переменные окружения уже добавлены в github variables, но их нужно также добавить в deploy
  - SENDSAY_API_URL
  - SENDSAY_API_KEY
5. Все операции отправки email сообщений теперь должны быть синхронные, следвоательно механизмы отправки почты через cron и через паттерн transactional outbox убирай
6. mailhog контейнер из compose.yml также убирай, он больше не нужен

---

Давай сделаем еще некоторое количество изменений
1. При вводе даты по slash command /date открывай datapicker из MUI X, сделай реэкспорт через @repo/ui в статическом варианте
2. В списке задач кнопки управления, которые находятся слева от каждого элемента не по вертикали по центру, сделай, чтобы в компоненте список задач в tiptap это было выровнено
3. При экспорте в pdf попрежему нет отступов от края, сделай отступы и проверь с помощью playwright
4. На странице /workspace/{workspaceId} в левом сайдбаре внизу, когда мы кликаем по профилю пользователя открывается меню, давай его поменяем
  - между Обновить план и выход добавь вертикальную верту
  - после настроек добавь Тема и там используй ButtonGroup variant="text"  size="small" и внутри иконки для системной темы, для светлой и для темной

для каждой заадчи запусти субагента
после проверь с помощью playwright