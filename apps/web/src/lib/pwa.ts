/**
 * Shared PWA constants: a single dark-neutral pair consistent with the brand
 * (`darkCanvas` in `@repo/ui` theme). The in-app theme stays dynamic — these
 * only color the install splash screen and the browser chrome.
 */
export const PWA_THEME_COLOR = '#262624'
export const PWA_BACKGROUND_COLOR = '#262624'

/** localStorage key for the one-time install banner dismissal. */
export const PWA_INSTALL_BANNER_DISMISS_KEY = 'anynote.pwa-install-banner.dismissed'
