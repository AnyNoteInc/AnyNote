<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron needs JIT for V8. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Electron ships pre-built frameworks signed with Apple's Team ID. Under an
       ad-hoc signature (mac.identity: "-", no Developer ID), library validation
       would reject loading those frameworks and the app would crash on launch.
       Disabling it lets the ad-hoc-signed app load them. Without a proper
       sealed/ad-hoc signature the bundle is reported "damaged" by Gatekeeper. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
